@COM_ABLE1021(ARG)
VARSET LOCAL
LOCAL = ARG
VARSET ARG

SIF TCVAR:MASTER:イベント
	RETURN 0
SIF CFLAG:LOCAL:現在位置 == 1 && CFLAG:LOCAL:現在地区 == 0 && TALENT:LOCAL:家族
	RETURN 0
SIF !HOLIDAY() && CFLAG:LOCAL:現在位置 == 2 && CFLAG:LOCAL:現在地区 == 1 && TIME >= 510 && TIME <= 749
	RETURN 0
SIF !HOLIDAY() && CFLAG:LOCAL:現在位置 == 2 && CFLAG:LOCAL:現在地区 == 1 && TIME >= 810 && TIME <= 959
	RETURN 0
SIF CFLAG:MASTER:部活 && CFLAG:LOCAL:現在位置 == CLUB_PLACE(CFLAG:MASTER:部活) && CFLAG:LOCAL:現在地区 == 1 && TIME >= 960 && TIME <= 1129
	RETURN 0
;SIF CFLAG:LOCAL:現在位置 != 18 || CFLAG:LOCAL:現在地区 != 2
;	RETURN 0
SIF LOCAL == MASTER || CFLAG:LOCAL:現在位置 != CFLAG:MASTER:現在位置 || CFLAG:LOCAL:現在地区 != CFLAG:MASTER:現在地区
	RETURN 0
RETURN 1
@COM1021(ARG)
VARSET LOCAL
PRINTL 
PRINTFORML %"=" * 107%
;襲いかかる
PRINTFORMW 突然%CALLNAME:ARG%袭击了过来
CALL EVENT_JOIN(ARG,MASTER,2)
;CFLAG:ARG:待機時間 += 10
VARSET ARG
RETURN 1

