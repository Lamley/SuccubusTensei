@COM_ABLE1002(ARG)
SIF !(CFLAG:MASTER:現在地区 == 0 && CFLAG:MASTER:現在位置 == 1)
	RETURN 0
RETURN GLOBAL_COMABLE(1002)


@COM1002(ARG)
PRINTL 
PRINTFORML %"=" * 107%
;勉強をする
CFLAG:MASTER:待機時間 = 60
EXP:MASTER:学習経験++
BASE:MASTER:学习成绩 = LIMIT(BASE:MASTER:学习成绩 + (ABL:MASTER:教養 + 1),0,1000)
DOWNBASE:ARG:体力 += CFLAG:ARG:待機時間 * 50 / 60
DOWNBASE:ARG:气力 += CFLAG:ARG:待機時間 * 100 / 60
DOWNBASE:ARG:ストレス -= CFLAG:ARG:待機時間 * 200 / 60
TB:(CFLAG:ARG:現在地区):(CFLAG:ARG:現在位置):ARG += 2
M:101 = 0
RETURN 1

