@COM_MESSAGE_1002
VARSET LOCAL
;===============================================================================
;通常コマンド
;　自慰などは一時分割
;===============================================================================
SIF M:101 == 1
	RETURN 0
;--------------------------------------------------------------------------
;勉強をする
;--------------------------------------------------------------------------
PRINTFORML ＜勉強をする＞
;日曜日ではなく、かつ時間が午前６時～９時
IF DAY % 7 != 0 && TIME / 60 >= 6 && TIME / 60 <= 9
	PRINTFORMW 在桌子前预习了今天的科目。
;日曜日ではなく、時間が16時（土曜日なら13時）以降
ELSEIF DAY % 7 != 0 && (TIME / 60 >= 16 || (TIME / 60 >= 13 && DAY % 7 == 6))
	IF RAND:2 == 0
		PRINTFORMW 在桌子前预习了明天的科目。
	ELSE
		PRINTFORMW 在桌子前复习了今天上的内容。
	ENDIF
;日曜日
ELSEIF DAY % 7 == 0
	PRINTFORMW 在桌子前预习了明天的科目。
ELSE
	PRINTFORMW 在桌子前重新学习了没有理解的部分。
ENDIF
M:101 = 1

