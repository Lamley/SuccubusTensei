@COM_MESSAGE_931
;--------------------------------------------------------------------------
;押さえつける
;--------------------------------------------------------------------------
PRINTFORML ＜钳住＞
SELECTCASE TFLAG:COM用変数
	CASE -1
		PRINTW 反抗不可
	CASE 0
		PRINTFORMW %CALLNAME:(TFLAG:行為者)%想钳住%CALLNAME:(TCVAR:(TFLAG:行為者):行為対象)%但失败了。
	CASE 1
		PRINTFORMW %CALLNAME:(TFLAG:行為者)%成功钳住了%CALLNAME:(TCVAR:(TFLAG:行為者):行為対象)%。
ENDSELECT

