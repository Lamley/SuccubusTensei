@COM_MESSAGE_996
;--------------------------------------------------------------------------
;同行してもらう
;--------------------------------------------------------------------------
IF !(TFLAG:成否用変数 == -2)
	PRINTFORML ＜同行してもらう＞
	PRINTFORML 跟%CALLNAME:(TCVAR:MASTER:行為対象)%一起行动了。
ENDIF

