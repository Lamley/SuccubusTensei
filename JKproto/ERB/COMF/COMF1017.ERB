@COM_ABLE1017(ARG)
SIF !(CFLAG:MASTER:現在位置 == CLUB_PLACE(CFLAG:MASTER:部活) && CFLAG:MASTER:現在地区 == 1)
	RETURN 0
SIF TIME >= 960 && TIME < 1130
	RETURN 0
RETURN GLOBAL_COMABLE(1017)


@COM1017(ARG)
PRINTL 
PRINTFORML %"=" * 107%
;くつろぐ（部活場所）

IF TIME > 930 && TIME < 960
	DOWNBASE:ARG:体力 -= 30 * (960 - TIME) / 30
	DOWNBASE:ARG:气力 -= 40 * (960 - TIME) / 30
	DOWNBASE:ARG:疲労 += 50 * (960 - TIME) / 30
	DOWNBASE:ARG:ストレス += 30 * (960 - TIME) / 30
	CFLAG:ARG:待機時間 = (960 - TIME)
ELSE
	DOWNBASE:ARG:体力 -= 30
	DOWNBASE:ARG:气力 -= 40
	DOWNBASE:ARG:疲労 += 50
	DOWNBASE:ARG:ストレス += 30
	CFLAG:ARG:待機時間 = 30
ENDIF
M:101 = 0

RETURN 1


