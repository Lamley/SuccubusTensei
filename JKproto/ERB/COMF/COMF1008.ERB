;[入浴][淋浴]

@COM_ABLE1008(ARG)
;自宅、NPCの家、社团活动楼、情人旅馆、宿舍、职业摔跤事务所以外は不可
SIF !((CFLAG:MASTER:現在地区 == 0 && CFLAG:MASTER:現在位置 == 1) | (CFLAG:MASTER:現在地区 == 1 && CFLAG:MASTER:現在位置 == 15) | (CFLAG:MASTER:現在地区 == 0 && CFLAG:MASTER:現在位置 == 30) | (CFLAG:MASTER:現在地区 == 2 && CFLAG:MASTER:現在位置 == 22) | (CFLAG:MASTER:現在地区 == 2 && CFLAG:MASTER:現在位置 == 26) | (CFLAG:MASTER:現在地区 == 2 && CFLAG:MASTER:現在位置 == 27))
	RETURN 0
RETURN GLOBAL_COMABLE(1008)

@COM1008(ARG)
PRINTL 
PRINTFORML %"=" * 107%

;体力／气力を10%回復
DOWNBASE:MASTER:体力 -= (MAXBASE:MASTER:体力 / 10)
DOWNBASE:MASTER:气力 -= (MAXBASE:MASTER:气力 / 5)
DOWNBASE:ARG:疲労 += 200
DOWNBASE:ARG:ストレス += 200

;PALAM[抑郁][不快][反感]を10%減少
PALAM:MASTER:30 -= (PALAM:MASTER:30 / 10)
PALAM:MASTER:31 -= (PALAM:MASTER:31 / 10)
PALAM:MASTER:32 -= (PALAM:MASTER:32 / 10)

CFLAG:MASTER:待機時間 = 60

;汚れを取り除く
;STAIN:MASTER:0 = 0
;STAIN:MASTER:1 = 0
;STAIN:MASTER:2 = 2
;STAIN:MASTER:3 = 1
;STAIN:MASTER:4 = 8
;STAIN:MASTER:5 = 0
VARSET STAIN:MASTER:0
M:101 = 0
RETURN 1
