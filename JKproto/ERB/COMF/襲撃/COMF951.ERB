@COM_ABLE951(ARG)
VARSET LOCAL
LOCAL = ARG
VARSET ARG
SIF TCVAR:LOCAL:イベント != 2 || BASE:(TCVAR:LOCAL:行為対象):反抗 == 0 || CFLAG:LOCAL:受能動 != 2
	RETURN 0
RETURN 1

@COM951(ARG)
VARSET LOCAL
;耐える
PRINTL 
PRINTFORML %"=" * 107%
IF BASE:ARG:反抗 == 0
	CFLAG:ARG:待機時間 += 2
	TFLAG:COM用変数 = -1
	RETURN 1
ENDIF
TFLAG:COM用変数 = 1
TCVAR:ARG:防御 = 1


CFLAG:ARG:待機時間 += 2
VARSET ARG

RETURN 1
