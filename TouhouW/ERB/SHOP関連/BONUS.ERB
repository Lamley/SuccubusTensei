@BONUS
#DIM 淫乳人数
#DIM 淫壺人数
#DIM 尻穴狂人数
#DIM 接吻魔人数
#DIM 恋慕人数
#DIM 最大好感度
VARSET LOCAL
淫乳人数 = 0
淫壺人数 = 0
尻穴狂人数 = 0
接吻魔人数 = 0
恋慕人数 = 0
最大好感度 = 0

FOR LOCAL,1,CHARANUM
SIF TALENT:LOCAL:淫乳 || GETBIT(CFLAG:LOCAL:引き継ぎ, 5)
	淫乳人数 ++
SIF TALENT:LOCAL:淫壺 || GETBIT(CFLAG:LOCAL:引き継ぎ, 3)
	淫壺人数 ++
SIF TALENT:LOCAL:尻穴狂 || GETBIT(CFLAG:LOCAL:引き継ぎ, 4)
	尻穴狂人数 ++
SIF TALENT:LOCAL:接吻魔 || GETBIT(CFLAG:LOCAL:引き継ぎ, 6)
	接吻魔人数 ++
SIF TALENT:LOCAL:恋慕 || GETBIT(CFLAG:LOCAL:引き継ぎ, 0) || GETBIT(CFLAG:LOCAL:引き継ぎ, 1)
	恋慕人数 ++
SIF CFLAG:LOCAL:2 > 最大好感度
	最大好感度 = CFLAG:LOCAL:2
NEXT

PRINTL 可以得到与完成的成就相応的额外奨励。（要至少进入一次收集界面才会反映収集状況）
DRAWLINE 

IF 最大好感度 < 100 && !GETBIT(FLAG:701,0)
	PRINT 解放条件：好感度100突破
ELSE
	PRINT [1] - 好感度100突破额外奨励：\500
ENDIF
SIF GETBIT(FLAG:701,0)
	PRINT 【済】
PRINTL
PRINTL
IF 最大好感度 < 500 && !GETBIT(FLAG:701,12)
	PRINT 解放条件：好感度500突破
ELSE
	PRINT [2] - 好感度500突破额外奨励：\3000
ENDIF
SIF GETBIT(FLAG:701,12)
	PRINT 【済】
PRINTL
PRINTL
IF 最大好感度 < 1000 && !GETBIT(FLAG:701,2)
	PRINT 解放条件：好感度1000突破
ELSE
	PRINT [3] - 好感度1000突破额外奨励：体力+100
ENDIF
SIF GETBIT(FLAG:701,2)
	PRINT 【済】
PRINTL
PRINTL
IF 最大好感度 < 10000 && !GETBIT(FLAG:701,3)
	PRINT 解放条件：好感度10000突破
ELSE
	PRINT [4] - 好感度10000突破额外奨励：【察知少女所在位置程度的能力】
ENDIF
SIF GETBIT(FLAG:701,3)
	PRINT 【済】
PRINTL
PRINTL
IF 最大好感度 < 30000 && !GETBIT(FLAG:701,5)
	PRINT 解放条件：好感度30000突破
ELSE
	PRINT [5] - 好感度30000突破额外奨励：【察知肮脏房间的程度的能力】
ENDIF
SIF GETBIT(FLAG:701,5)
	PRINT 【済】
PRINTL
PRINTL
IF 最大好感度 < 50000 && !GETBIT(FLAG:701,9)
	PRINT 解放条件：好感度50000突破
ELSE
	PRINT [6] - 好感度50000突破额外奨励：【隠秘携帯内袴程度的能力】
ENDIF
SIF GETBIT(FLAG:701,9)
	PRINT 【済】
PRINTL
PRINTL
IF FLAG:獲得パンツ種数 < 10 && !GETBIT(FLAG:701,1)
	PRINT 解放条件：收集10種内袴
ELSE
	PRINT [32] - 收集10種内袴额外奨励：清掃経験+30
ENDIF
SIF GETBIT(FLAG:701,1)
	PRINT 【済】
PRINTL
PRINTL
IF FLAG:獲得パンツ種数 < 30 && !GETBIT(FLAG:701,16)
	PRINT 解放条件：收集30種内袴
ELSE
	PRINT [33] - 收集30種内袴额外奨励：【看出欲求不満的少女程度的能力】
ENDIF
SIF GETBIT(FLAG:701,16)
	PRINT 【済】
PRINTL
PRINTL
IF FLAG:獲得パンツ種数 < 50 && !GETBIT(FLAG:701,4)
	PRINT 解放条件：收集50種内袴
ELSE
	PRINT [35] - 收集50種内袴额外奨励：体力+300
ENDIF
SIF GETBIT(FLAG:701,4)
	PRINT 【済】
PRINTL
PRINTL
IF FLAG:獲得パンツ種数 < 100 && !GETBIT(FLAG:701,13)
	PRINT 解放条件：收集100種内袴
ELSE
	PRINT [36] - 收集100種内袴额外奨励：「祭壇」入手
ENDIF
SIF GETBIT(FLAG:701,13)
	PRINT 【済】
PRINTL
PRINTL
IF FLAG:獲得パンツ種数 < 150 && !GETBIT(FLAG:701,15)
	PRINT 解放条件：收集150種内袴
ELSE
	PRINT [37] - 收集150種内袴额外奨励：【找出欲求不満的少女程度的能力】
ENDIF
SIF GETBIT(FLAG:701,15)
	PRINT 【済】
PRINTL
PRINTL
IF FLAG:獲得パンツ種数 < 200 && !GETBIT(FLAG:701,7)
	PRINT 解放条件：收集200種内袴
ELSE
	PRINT [38] - 收集200種内袴额外奨励：体力+500
ENDIF
SIF GETBIT(FLAG:701,7)
	PRINT 【済】
PRINTL
PRINTL
IF FLAG:獲得パンツ種数 < 250 && !GETBIT(FLAG:701,6)
	PRINT 解放条件：收集250種内袴
ELSE
	PRINT [39] - 收集250種内袴额外奨励：【回復快】
ENDIF
SIF GETBIT(FLAG:701,6)
	PRINT 【済】
PRINTL
PRINTL
IF FLAG:獲得パンツ種数 < 300 && !GETBIT(FLAG:701,8)
	PRINT 解放条件：收集300種内袴
ELSE
	PRINT [40] - 收集300種内袴额外奨励：【千客万来】
ENDIF
SIF GETBIT(FLAG:701,8)
	PRINT 【済】
PRINTL
PRINTL
IF ABL:MASTER:料理技能 < 3
	PRINT 解放条件：主人公的料理3以上
ELSE
	PRINT [45] - 料理奨励：【食量大】
ENDIF
SIF GETBIT(FLAG:701,14)
	PRINT 【済】
PRINTL
PRINTL
IF ABL:MASTER:指 < 5 || ABL:MASTER:舌 < 5
	PRINT 解放条件：主人公的指＆舌技5以上
ELSE
	PRINT [51] - 愛撫大师额外奨励：【Ｃ鈍感無視】
ENDIF
SIF GETBIT(FLAG:702,0)
	PRINT 【済】
PRINTL
PRINTL
IF 淫乳人数 < 5
	PRINT 解放条件：淫乳5人
ELSE
	PRINT [52] - 淫乳5人额外奨励：【Ｂ鈍感無視】
ENDIF
SIF GETBIT(FLAG:702,1)
	PRINT 【済】
PRINTL
PRINTL
IF 淫壺人数 < 5
	PRINT 解放条件：淫壺5人
ELSE
	PRINT [53] - 淫壺5人额外奨励：【Ｖ鈍感無視】
ENDIF
SIF GETBIT(FLAG:702,2)
	PRINT 【済】
PRINTL
PRINTL
IF 尻穴狂人数 < 5
	PRINT 解放条件：尻穴狂5人
ELSE
	PRINT [54] - 尻穴狂5人额外奨励：【Ａ鈍感無視】
ENDIF
SIF GETBIT(FLAG:702,3)
	PRINT 【済】
PRINTL
PRINTL
IF 接吻魔人数 < 5
	PRINT 解放条件：接吻魔5人
ELSE
	PRINT [55] - 接吻魔5人额外奨励：【魅惑】
ENDIF
SIF GETBIT(FLAG:702,4)
	PRINT 【済】
PRINTL
PRINTL
IF !恋慕人数
	PRINT 解放条件：獲得恋慕
ELSE
	PRINT [56] - 恋慕额外奨励：気力+100
ENDIF
SIF GETBIT(FLAG:702,5)
	PRINT 【済】
PRINTL
PRINTL
IF 恋慕人数 < 5
	PRINT 解放条件：恋慕5人
ELSE
	PRINT [57] - 恋慕5人额外奨励：生理周期可視化
ENDIF
SIF FLAG:1007
	PRINT 【ON】
PRINTL
PRINTL
IF 恋慕人数 < 10
	PRINT 解放条件：恋慕10人
ELSE
	PRINT [58] - 恋慕10人额外奨励：気力+300
ENDIF
SIF GETBIT(FLAG:702,6)
	PRINT 【済】
PRINTL
PRINTL
IF 恋慕人数 < 20
	PRINT 解放条件：恋慕20人
ELSE
	PRINT [59] - 恋慕20人额外奨励：【謎之魅力】
ENDIF
SIF GETBIT(FLAG:702,7)
	PRINT 【済】
PRINTL
PRINTL
IF EXP:MASTER:時姦経験 < 100
	PRINT 解放条件：時姦経験100
ELSE
	PRINT [100] - 時姦経験100额外奨励：精力+100
ENDIF
SIF GETBIT(FLAG:702,8)
	PRINT 【済】
PRINTL
PRINTL
IF !TALENT:MASTER:讓巫女懷孕 || !TALENT:MASTER:讓妖精懷孕 || !TALENT:MASTER:讓妖怪懷孕 || !TALENT:MASTER:讓神霊懷孕
	PRINT 解放条件：让所有种族都怀孕过
ELSE
	PRINT [200] - 怀孕素质全取得奨励：【让人怀孕的能手】
ENDIF
SIF GETBIT(FLAG:702,11)
	PRINT 【済】
PRINTL
PRINTL

PRINT [0] - 返回

INPUT
IF RESULT == 1 && (GETBIT(FLAG:701,0) || 最大好感度 >= 100)
	IF !GETBIT (FLAG:701,0)
		PRINTL 獲得了\500。
		MONEY += 500
		SETBIT FLAG:701,0
	ELSE
		PRINTL 選択完成。
	ENDIF
	WAIT
	RESTART
ELSEIF RESULT == 2 && (GETBIT(FLAG:701,12) || 最大好感度 >= 500)
	IF !GETBIT (FLAG:701,12)
		PRINTL 獲得了\3000。
		MONEY += 3000
		SETBIT FLAG:701,12
	ELSE
		PRINTL 選択完成。
	ENDIF
	WAIT
	RESTART
ELSEIF RESULT == 3 && (GETBIT(FLAG:701,2) || 最大好感度 >= 1000)
	IF !GETBIT(FLAG:701,2)
		PRINTL 体力的上限提升了。
		MAXBASE:MASTER:体力 += 100
		SETBIT FLAG:701,2
	ELSE
		PRINTL 選択完成。
	ENDIF
	WAIT
	RESTART
ELSEIF RESULT == 4 && (GETBIT(FLAG:701,3) || 最大好感度 >= 10000)
	IF !GETBIT(FLAG:701,3)
		PRINTL 获得【察知少女所在位置程度的能力】。
		TALENT:MASTER:居場所察知 = 1
		SETBIT FLAG:701,3
	ELSE
		PRINTL 選択完成。
	ENDIF
	WAIT
	RESTART

ELSEIF RESULT == 5 && (GETBIT(FLAG:701,5) || 最大好感度 >= 30000)
	IF !GETBIT(FLAG:701,5)
		PRINTL 获得【察知肮脏房间的程度的能力】。
		TALENT:MASTER:汚部屋察知 = 1
		SETBIT FLAG:701,5
	ELSE
		PRINTL 選択完成。
	ENDIF
	WAIT
	RESTART
ELSEIF RESULT == 6 && (GETBIT(FLAG:701,9) || 最大好感度 >= 50000)
	IF !GETBIT(FLAG:701,9)
		PRINTL 获得【内袴走私者】。変得容易回避搜身。
		TALENT:MASTER:内袴走私者 = 1
		SETBIT FLAG:701,9
	ELSE
		PRINTL 選択完成。
	ENDIF
	WAIT
	RESTART
ELSEIF RESULT == 32 && FLAG:獲得パンツ種数 >= 10
	IF !GETBIT(FLAG:701,1)
		PRINTL 获得清掃経験。
		EXP:MASTER:清掃経験 += 30
		SETBIT FLAG:701,1
	ELSE
		PRINTL 選択完成。
	ENDIF
	WAIT
	RESTART
ELSEIF RESULT == 33 && FLAG:獲得パンツ種数 >= 30
	IF !GETBIT(FLAG:701,16)
		PRINTFORML 因為收集了许多胖次，神给与了%CALLNAME:MASTER%奇迹般的能力
		PRINTFORML %CALLNAME:MASTER%得到了【看出欲求不満的少女程度】的能力
		SIF TALENT:MASTER:劣情察知 < 1
			TALENT:MASTER:劣情察知 = 1
		SETBIT FLAG:701,16
	ELSE
		PRINTL 選択完成。
	ENDIF
	WAIT
	RESTART
ELSEIF RESULT == 35 && FLAG:獲得パンツ種数 >= 50
	IF !GETBIT(FLAG:701,4)
		PRINTL 体力的上限提升了。
		MAXBASE:MASTER:体力 += 300
		SETBIT FLAG:701,4
	ELSE
		PRINTL 選択完成。
	ENDIF
	WAIT
	RESTART
ELSEIF RESULT == 36 && FLAG:獲得パンツ種数 >= 100
	IF !GETBIT(FLAG:701,13)
		PRINTL 為了更好的收集胖次而被赐予祭壇。
		PRINTFORML 可以在自室祈禱和聽天之声
		ITEM:祭壇 = 1
		SETBIT FLAG:701,13
	ELSE
		PRINTL 選択完成。
	ENDIF
	WAIT
	RESTART
ELSEIF RESULT == 37 && FLAG:獲得パンツ種数 >= 150
	IF !GETBIT(FLAG:701,15)
		PRINTFORML 因為收集了大量的胖次，所以神让%CALLNAME:MASTER%的能力変强了
		PRINTFORML %CALLNAME:MASTER%得到了【找出欲求不満的少女程度】的能力
			TALENT:MASTER:劣情察知 = 2
		SETBIT FLAG:701,15
	ELSE
		PRINTL 選択完成。
	ENDIF
	WAIT
	RESTART
ELSEIF RESULT == 38 && FLAG:獲得パンツ種数 >= 200
	IF !GETBIT(FLAG:701,7)
		PRINTL 体力的上限提升了。
		MAXBASE:MASTER:体力 += 500
		SETBIT FLAG:701,7
	ELSE
		PRINTL 選択完成。
	ENDIF
	WAIT
	RESTART
ELSEIF RESULT == 39 && FLAG:獲得パンツ種数 >= 250
	IF !GETBIT(FLAG:701,6)
		PRINTL 获得【回復快】。
		TALENT:MASTER:回復速度 = 1
		SETBIT FLAG:701,6
	ELSE
		PRINTL 選択完成。
	ENDIF
	WAIT
	RESTART
ELSEIF RESULT == 40 && FLAG:獲得パンツ種数 >= 300
	IF !GETBIT (FLAG:701,8)
		PRINTL 获得【千客万来】。
		TALENT:MASTER:千客万来 = 1
		SETBIT FLAG:701,8
	ELSE
		PRINTL 選択完成。
	ENDIF
	WAIT
	RESTART
ELSEIF RESULT == 45 && ABL:MASTER:料理技能 >= 3
	IF !GETBIT (FLAG:701,14)
		PRINTL 获得【食量大】。
		TALENT:MASTER:食量大 = 1
		SETBIT FLAG:701,14
	ELSE
		PRINTL 選択完成。
	ENDIF
	WAIT
	RESTART
ELSEIF RESULT == 51 && ABL:MASTER:指 > 4 && ABL:MASTER:舌 > 4
	IF !GETBIT (FLAG:702,0)
		PRINTL 获得【Ｃ鈍感無視】。
		TALENT:MASTER:Ｃ鈍感無視 = 1
		SETBIT FLAG:702,0
	ELSE
		PRINTL 選択完成。
	ENDIF
	WAIT
	RESTART
ELSEIF RESULT == 52 && 淫乳人数 > 4
	IF !GETBIT (FLAG:702,1)
		PRINTL 获得【Ｂ鈍感無視】。
		TALENT:MASTER:Ｂ鈍感無視 = 1
		SETBIT FLAG:702,1
	ELSE
		PRINTL 選択完成。
	ENDIF
	WAIT
	RESTART
ELSEIF RESULT == 53 && 淫壺人数 > 4
	IF !GETBIT (FLAG:702,2)
		PRINTL 获得【Ｖ鈍感無視】。
		TALENT:MASTER:Ｖ鈍感無視 = 1
		SETBIT FLAG:702,2
	ELSE
		PRINTL 選択完成。
	ENDIF
	WAIT
	RESTART
ELSEIF RESULT == 54 && 尻穴狂人数 > 4
	IF !GETBIT (FLAG:702,3)
		PRINTL 获得【Ａ鈍感無視】。
		TALENT:MASTER:Ａ鈍感無視 = 1
		SETBIT FLAG:702,3
	ELSE
		PRINTL 選択完成。
	ENDIF
	WAIT
	RESTART
ELSEIF RESULT == 55 && 接吻魔人数 > 4
	IF !GETBIT (FLAG:702,4)
		PRINTL 获得【魅惑】。
		TALENT:MASTER:魅惑 = 1
		SETBIT FLAG:702,4
	ELSE
		PRINTL 選択完成。
	ENDIF
	WAIT
	RESTART
ELSEIF RESULT == 56 && 恋慕人数
	IF !GETBIT (FLAG:702,5)
		PRINTL 気力的上限提升了。
		MAXBASE:MASTER:気力 += 100
		SETBIT FLAG:702,5
	ELSE
		PRINTL 選択完成。
	ENDIF
	WAIT
	RESTART
ELSEIF RESULT == 57 && (恋慕人数 > 4 || GETBIT (FLAG:702,10))
	IF !FLAG:1007 
		PRINTL 変得能知道生理周期的样子。
		FLAG:1007 = 1
		SETBIT FLAG:702,10
	ELSE
		PRINTL 无法看到生理周期了。
		FLAG:1007 = 0
	ENDIF
	WAIT
	RESTART
ELSEIF RESULT == 58 && 恋慕人数 > 9
	IF !GETBIT (FLAG:702,6)
		PRINTL 気力的上限提升了。
		MAXBASE:MASTER:気力 += 300
		SETBIT FLAG:702,6
	ELSE
		PRINTL 選択完成。
	ENDIF
	WAIT
	RESTART
ELSEIF RESULT == 59 && 恋慕人数 > 19
	IF !GETBIT (FLAG:702,7)
		PRINTL 获得【謎之魅力】。
		TALENT:MASTER:謎之魅力 = 1
		SETBIT FLAG:702,7
	ELSE
		PRINTL 選択完成。
	ENDIF
	WAIT
	RESTART
ELSEIF RESULT == 100 && EXP:MASTER:時姦経験 >= 100
	IF !GETBIT (FLAG:702,8)
		PRINTL 精力的上限提升了。
		MAXBASE:MASTER:精力 += 100
		SETBIT FLAG:702,8
	ELSE
		PRINTL 選択完成。
	ENDIF
	WAIT
	RESTART
ELSEIF RESULT == 200 && TALENT:MASTER:讓巫女懷孕 && TALENT:MASTER:讓妖精懷孕 && TALENT:MASTER:讓妖怪懷孕 && TALENT:MASTER:讓神霊懷孕
	IF !GETBIT (FLAG:702,11)
		PRINTL 獲得【让人怀孕的能手】。
		PRINTFORMW 现在恐怕连【非生物】和【蓬莱人】都会因為%CALLNAME:MASTER%的精子而怀孕了。
		TALENT:MASTER:讓人懷孕的能手 = 1
		SETBIT FLAG:702,11
	ELSE
		PRINTL 選択完成。
	ENDIF
	WAIT
	RESTART
ELSE
	RETURN 0
ENDIF