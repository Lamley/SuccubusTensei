@DAIRY_CONDITION_EV18
SIF RAND:15 > 0
	RETURN -1
CALL DAIRY_EV18
RETURN 1

@DAIRY_EV18
PRINTFORMW 從月亮上灑落着妖異的光芒……

FOR LOCAL,0,CHARANUM
	SIF ((TALENT:LOCAL:150 || TALENT:LOCAL:151) && !TALENT:LOCAL:初潮) || !GETBIT(TALENT:LOCAL:性別,0)
		CONTINUE
	SIF RAND:5 > 0
		CONTINUE
	CALL COLORMESSAGE(@"%CALLNAME:LOCAL%的生理周期紊乱了",C_YELLOW,1)
	CFLAG:LOCAL:900 = RAND:10 + 1
NEXT
