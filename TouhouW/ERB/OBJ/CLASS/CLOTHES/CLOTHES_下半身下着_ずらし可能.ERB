;要素名→ID変換用関数の存否確認用関数
;変換可能（IDと要素名の関係が常に変化しない）なら設置
;状況によって名前が変わるようなのは不可能
;WEAPONIDは変換可能
@EXIST_下半身内袴_ずらし可能_ID
#LOCALSIZE 1
#LOCALSSIZE 1

;要素名→ID変換用関数
;必要なら各オブジェクトで同様に作成のこと
@下半身内袴_ずらし可能_ID(ARG:0, ARGS:0, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS V_NAME                  ;作成する変数名
#DIMS C_NAME, OBJ_ID_LAST + 1 ;データ保持
#DIM  O_ID                    ;参照用
IF ARGS:0 == "RESET"
	VARSET C_NAME
	FOR O_ID, 1, OBJ_ID_LAST + 1
		C_NAME:O_ID = %GET_STR(, "下半身内袴_ずらし可能", O_ID, "名前")%
	NEXT
ELSE
	O_ID = FINDELEMENT(C_NAME, ARGS:0, , , 1)
	CALLF MAKE_INT(V_NAME, O_ID)
ENDIF



;存否関数
;F関数はTRYCALL系が使えないので通常関数で存否配列を作っちゃう
@EXIST_下半身内袴_ずらし可能0
#LOCALSIZE 1
#LOCALSSIZE 1

;オブジェクト本体
@下半身内袴_ずらし可能0(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	;STR型の一時変数を定義する命令
	CALLF MAKE_STR(V_NAME, "ノーパン")
CASE "はける"
;ノーパンを履くとは
	SIF TALENT:ARG:小悪魔 || TALENT:ARG:淫乱
		CALLF MAKE_INT(V_NAME, 1)
ENDSELECT



@EXIST_下半身内袴_ずらし可能1
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内袴_ずらし可能1(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "四角袴")
CASE "描写"
	CALLF MAKE_STR(V_NAME, "男式")
CASE "換装可能"
	CALLF MAKE_INT(V_NAME, 1)
ENDSELECT



@EXIST_下半身内袴_ずらし可能2

@下半身内袴_ずらし可能2(ARG, O_DATA, V_NAME)
#FUNCTION
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "襯袴")
CASE "描写"
	CALLF MAKE_STR(V_NAME, "構造寬鬆的")
CASE "はける"
	CALLF MAKE_INT(V_NAME, 1)
ENDSELECT



@EXIST_下半身内袴_ずらし可能3
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内袴_ずらし可能3(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "小熊胖次")
CASE "描写"
	CALLF MAKE_STR(V_NAME, "印着吉祥物的可愛的")
CASE "はける"
	IF TALENT:ARG:小悪魔 || TALENT:ARG:淫乱
	ELSEIF TALENT:ARG:幼稚 || TALENT:ARG:幼児／幼児退行
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
ENDSELECT



@EXIST_下半身内袴_ずらし可能4
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内袴_ずらし可能4(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "水珠胖次")
CASE "描写"
	CALLF MAKE_STR(V_NAME, "孩子気的")
CASE "はける"
	IF TALENT:ARG:小悪魔 || TALENT:ARG:淫乱
	ELSEIF TALENT:ARG:幼稚 || TALENT:ARG:幼児／幼児退行 || TALENT:ARG:体型 < 0
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
ENDSELECT



@EXIST_下半身内袴_ずらし可能5
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内袴_ずらし可能5(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "条紋胖次")
CASE "描写"
	CALLF MAKE_STR(V_NAME, "凸顯出臀部曲線的")
CASE "はける"
	CALLF MAKE_INT(V_NAME, 1)
ENDSELECT



@EXIST_下半身内袴_ずらし可能6
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内袴_ずらし可能6(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "褶辺胖次")
CASE "描写"
	CALLF MAKE_STR(V_NAME, "装饰着可愛褶辺的")
CASE "はける"
	IF TALENT:ARG:小悪魔 || TALENT:ARG:淫乱
	ELSEIF TALENT:ARG:幼稚 || TALENT:ARG:幼児／幼児退行 || TALENT:ARG:体型 < 0
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
ENDSELECT



@EXIST_下半身内袴_ずらし可能7
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内袴_ずらし可能7(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "純色胖次")
CASE "描写"
	CALLF MAKE_STR(V_NAME, "毫不做作的")
CASE "はける"
	IF TALENT:ARG:小悪魔 || TALENT:ARG:淫乱
	ELSE
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
ENDSELECT



@EXIST_下半身内袴_ずらし可能10
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内袴_ずらし可能10(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "低腰胖次")
CASE "描写"
	CALLF MAKE_STR(V_NAME, "設計質樸的")
CASE "はける"
	CALLF MAKE_INT(V_NAME, 1)
ENDSELECT



@EXIST_下半身内袴_ずらし可能11
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内袴_ずらし可能11(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "低腰緊身胖次")
CASE "描写"
	CALLF MAKE_STR(V_NAME, "仿佛能看到股溝的")
CASE "はける"
	SIF !RAND:2 && (TALENT:ARG:小悪魔 || TALENT:ARG:淫乱)
		CALLF MAKE_INT(V_NAME, 1)
ENDSELECT



@EXIST_下半身内袴_ずらし可能12
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内袴_ずらし可能12(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "高開叉胖次")
CASE "描写"
	CALLF MAKE_STR(V_NAME, "格外修長的")
CASE "はける"
	SIF !RAND:2 && (TALENT:ARG:小悪魔 || TALENT:ARG:淫乱)
		CALLF MAKE_INT(V_NAME, 1)
ENDSELECT



@EXIST_下半身内袴_ずらし可能13
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内袴_ずらし可能13(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "蕾絲胖次（白）")
CASE "描写"
	CALLF MAKE_STR(V_NAME, "整潔并有些透明的")
CASE "はける"
	IF TALENT:ARG:小悪魔 || TALENT:ARG:淫乱
		CALLF MAKE_INT(V_NAME, 1)
	ELSEIF TALENT:ARG:幼稚 || TALENT:ARG:幼児／幼児退行 || TALENT:ARG:体型
		CALLF MAKE_INT(V_NAME, 0)
	ELSE
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
ENDSELECT



@EXIST_下半身内袴_ずらし可能14
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内袴_ずらし可能14(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	;STR型の一時変数を定義する命令
	CALLF MAKE_STR(V_NAME, "蕾絲胖次（黒）")
CASE "描写"
	CALLF MAKE_STR(V_NAME, "充満成熟女人韻味的")
CASE "はける"
	IF TALENT:ARG:小悪魔 || TALENT:ARG:淫乱
		CALLF MAKE_INT(V_NAME, 1)
	ELSEIF TALENT:ARG:幼稚 || TALENT:ARG:幼児／幼児退行 || TALENT:ARG:体型
		CALLF MAKE_INT(V_NAME, 0)
	ELSE
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
ENDSELECT



@EXIST_下半身内袴_ずらし可能15
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内袴_ずらし可能15(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "Ｔ字袴")
CASE "描写"
	CALLF MAKE_STR(V_NAME, "裸露大片臀部的大胆的")
CASE "はける"
	SIF TALENT:ARG:恋慕
		CALLF MAKE_INT(V_NAME, 1)
ENDSELECT



@EXIST_下半身内袴_ずらし可能16
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内袴_ずらし可能16(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "縄系胖次")
CASE "描写"
	CALLF MAKE_STR(V_NAME, "用細縄系在腰上的")
CASE "はける"
	SIF TALENT:ARG:恋慕
		CALLF MAKE_INT(V_NAME, 1)
ENDSELECT



@EXIST_下半身内袴_ずらし可能17
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内袴_ずらし可能17(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "皮革胖次")
CASE "描写"
	CALLF MAKE_STR(V_NAME, "縫着豎向拉鏈的")
CASE "はける"
	SIF TALENT:ARG:恋慕 && ABL:ARG:施虐属性 > 4
		CALLF MAKE_INT(V_NAME, 1)
ENDSELECT



@EXIST_下半身内袴_ずらし可能18
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内袴_ずらし可能18(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "虎皮胖次")
CASE "描写"
	CALLF MAKE_STR(V_NAME, "与鬼族少女相称的")
CASE "はける"
	CALLF MAKE_INT(V_NAME, 1)
ENDSELECT



@EXIST_下半身内袴_ずらし可能19
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内袴_ずらし可能19(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "三角胖次")
CASE "描写"
	CALLF MAKE_STR(V_NAME, "充満少女味的可愛的")
CASE "はける"
	CALLF MAKE_INT(V_NAME, 1)
ENDSELECT



@EXIST_下半身内袴_ずらし可能20
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内袴_ずらし可能20(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "超短胖次")
CASE "描写"
	CALLF MAKE_STR(V_NAME, "極為短小的")
CASE "はける"
	SIF FLAG:セクシーパンツ追加
		CALLF MAKE_INT(V_NAME, 1)
ENDSELECT



@EXIST_下半身内袴_ずらし可能21
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内袴_ずらし可能21(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "兜襠布")
CASE "描写"
	CALLF MAKE_STR(V_NAME, "緊緊抱住屁股的")
CASE "はける"
	SIF FLAG:セクシーパンツ追加
		CALLF MAKE_INT(V_NAME, 1)
ENDSELECT



@EXIST_下半身内袴_ずらし可能22
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内袴_ずらし可能22(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "丁字袴")
CASE "描写"
	CALLF MAKE_STR(V_NAME, "面積極小简直跟縄一樣的")
CASE "はける"
	IF !(ABL:ARG:露出癖 >= 3 && ABL:ARG:欲望 >= 10 && FLAG:セクシーパンツ追加)
		CALLF MAKE_INT(V_NAME, 0)
	ELSEIF TALENT:ARG:小悪魔 || TALENT:ARG:淫乱
		CALLF MAKE_INT(V_NAME, 1)
	ELSEIF TALENT:ARG:幼稚 || TALENT:ARG:幼児／幼児退行 || TALENT:ARG:体型
		CALLF MAKE_INT(V_NAME, 0)
	ELSE
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
ENDSELECT



@EXIST_下半身内袴_ずらし可能23
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内袴_ずらし可能23(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "透明胖次")
CASE "描写"
	CALLF MAKE_STR(V_NAME, "重要的部位能直接看見的")
CASE "はける"
	IF !(ABL:ARG:露出癖 >= 3 && ABL:ARG:欲望 >= 10 && FLAG:セクシーパンツ追加)
		CALLF MAKE_INT(V_NAME, 0)
	ELSEIF TALENT:ARG:小悪魔 || TALENT:ARG:淫乱
		CALLF MAKE_INT(V_NAME, 1)
	ELSEIF TALENT:ARG:幼稚 || TALENT:ARG:幼児／幼児退行 || TALENT:ARG:体型
		CALLF MAKE_INT(V_NAME, 0)
	ELSE
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
ENDSELECT



@EXIST_下半身内袴_ずらし可能24
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内袴_ずらし可能24(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "開放式胖次")
CASE "描写"
	CALLF MAKE_STR(V_NAME, "清楚的暴露出恥丘的")
CASE "はける"
	IF !(ABL:ARG:露出癖 >= 3 && ABL:ARG:欲望 >= 10 && FLAG:セクシーパンツ追加)
		CALLF MAKE_INT(V_NAME, 0)
	ELSEIF TALENT:ARG:小悪魔 || TALENT:ARG:淫乱
		CALLF MAKE_INT(V_NAME, 1)
	ELSEIF TALENT:ARG:幼稚 || TALENT:ARG:幼児／幼児退行 || TALENT:ARG:体型
		CALLF MAKE_INT(V_NAME, 0)
	ELSE
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
ENDSELECT



@EXIST_下半身内袴_ずらし可能25
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内袴_ずらし可能25(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "創口貼")
CASE "描写"
	CALLF MAKE_STR(V_NAME, "沿着裂縫軽軽貼上的")
CASE "はける"
	IF !(ABL:ARG:露出癖 >= 3 && ABL:ARG:欲望 >= 10 && FLAG:セクシーパンツ追加)
		CALLF MAKE_INT(V_NAME, 0)
	ELSEIF TALENT:ARG:小悪魔 || TALENT:ARG:淫乱
		CALLF MAKE_INT(V_NAME, 0)
	ELSEIF TALENT:ARG:幼稚 || TALENT:ARG:幼児／幼児退行 || TALENT:ARG:体型
		CALLF MAKE_INT(V_NAME, 1)
	ELSE
		CALLF MAKE_INT(V_NAME, 0)
	ENDIF
ENDSELECT



@EXIST_下半身内袴_ずらし可能26
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内袴_ずらし可能26(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "襯袴（開襠）")
CASE "描写"
	CALLF MAKE_STR(V_NAME, "為了排便在胯下有着很大空隙的")
CASE "はける"
	IF !(ABL:ARG:露出癖 >= 3 && ABL:ARG:欲望 >= 10 && FLAG:セクシーパンツ追加)
		CALLF MAKE_INT(V_NAME, 0)
	ELSE
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
ENDSELECT



@EXIST_下半身内袴_ずらし可能27
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内袴_ずらし可能27(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "貞操帯")
CASE "描写"
	CALLF MAKE_STR(V_NAME, "掛着牢固的鎖的金属製的")
ENDSELECT



@EXIST_下半身内袴_ずらし可能28
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内袴_ずらし可能28(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "触手胖次")
CASE "描写"
	CALLF MAKE_STR(V_NAME, "内側有什麼在蠢動着的")
ENDSELECT



@EXIST_下半身内袴_ずらし可能29
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内袴_ずらし可能29(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "振動棒胖次")
CASE "描写"
	CALLF MAKE_STR(V_NAME, "放入円筒状異物的")
ENDSELECT



@EXIST_下半身内袴_ずらし可能30
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内袴_ずらし可能30(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "二穴振動棒胖次")
CASE "描写"
	CALLF MAKE_STR(V_NAME, "兩根棒子在腿間嗡嗡作響的")
ENDSELECT



@EXIST_下半身内袴_ずらし可能31
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内袴_ずらし可能31(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "比基尼内袴")
;下半身内袴２の20
CASE "換装可能"
	CALLF MAKE_INT(V_NAME, 1)
ENDSELECT



@EXIST_下半身内袴_ずらし可能32
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内袴_ずらし可能32(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "微型内袴")
;下半身内袴２の21
CASE "換装可能"
	CALLF MAKE_INT(V_NAME, 1)
ENDSELECT



