@COM616
PRINTL 「今天来这有什么事吗？」
$LOOP
DRAWLINE
PRINTL [0] 想要驱除疲労…
PRINTL [1] 总感覚精神很憔悴…
;SIF MAXBASE:MASTER:精力 - BASE:MASTER:精力 > 500
;	PRINTL [2] 彼女が激しくて…
PRINTL [3] 最近和女友间缺乏激情…
SIF TALENT:MASTER:清掃中毒
	PRINTL [4] 太喜欢掃除到根本停不下来
;PRINTL [5] 先生の事を考えるとおちんちんがおっきくなっちゃうんです
;PRINTL [5] 先生の事を考えるとお股がムズムズするんです
;SIF GETBIT(TALENT:MASTER:性別 & 1)
;PRINTL [10] おちんちんの悩み
DRAWLINE
PRINTL [100] 中止
INPUT
IF RESULT == 0
	CALL REMEDY(RAND:3)
	PRINTL 被要求支付治療費\3000
	IF MONEY < 3000
		DOWNBASE:MASTER:体力 -= 100
		DOWNBASE:MASTER:気力 -= 200
		TIME += 180
		TFLAG:193 = 1
	ELSE
		MONEY -= 3000
	ENDIF
ELSEIF RESULT == 1
	;永琳の思慕恋慕で良化
	CALL REMEDY(RAND:2 + 3 + TALENT:(NO:72):3 * 2 + TALENT:(NO:72):8)
	PRINTL 被要求支付治療費\3000
	IF MONEY < 3000
		DOWNBASE:MASTER:体力 -= 100
		DOWNBASE:MASTER:気力 -= 200
		TIME += 180
		TFLAG:193 = 1
	ELSE
		MONEY -= 3000
	ENDIF
ELSEIF RESULT == 3
	PRINTL 「要不要试试这个」
	DRAWLINE
	$SELL
	FOR LOCAL,12,17
		SIF ITEMNAME:LOCAL == ""
			CONTINUE
		PRINTFORM [{LOCAL}] %ITEMNAME:LOCAL% ￥{(ITEMPRICE:LOCAL) / 5}
		IF ITEM:LOCAL
			PRINTL 　（卖完了）
		ELSE
			PRINTL
		ENDIF
	NEXT
	PRINTL
	PRINTL [100] 返回
	$LOOP2
	INPUT
	IF RESULT >= 12 && RESULT <= 17
		CALL BUY_ABLE(RESULT,0,1,80)
		GOTO SELL
	ELSEIF RESULT == 100
		RETURN 0
	ELSE
		GOTO LOOP2
	ENDIF
ELSEIF RESULT == 4 && TALENT:MASTER:清掃中毒
	IF FLAG:850 == 0
		PRINTL 「喜欢做家务？这不是好事吗」
		PRINTL 「并不是这样？」
		PRINTW 「导致不能专心去做别的事？这是挺令人困扰的呢」
		PRINTW …
		PRINTW 「嗯ー、的确是有一点異常呢」
		PRINTW ……
		PRINTW 「还伴有性欲上的興奮！？」
		PRINTW …………
		PRINTL 「到底是做了什么才会変成这种状况呢」
		PRINTL 「因為让我见识到了罕见的症例所以这次就免费為你治疗吧」
		PRINTL 「今後不要再进行奇怪的掃除了」
		PRINTFORMW %CALLNAME:MASTER%的[清掃中毒]被治好了
		TALENT:MASTER:清掃中毒 = 0
		FLAG:850 ++
	ELSE
		PRINTL 「那个病又复発了吗？」
		PRINTFORML 「真是的… 这次就收你￥{FLAG:850 * 10000}吧」
		IF MONEY < FLAG:850 * 10000
			PRINTL 「看起来没有足够的治療費呢」
			PRINTL 「請存好钱再来吧」
		ELSE
			PRINTW …
			PRINTW ……
			PRINTW …………
			PRINTL 「嗯、治好了」
			PRINTL 「要是再复発的话可得加价哦」
			PRINTFORMW %CALLNAME:MASTER%的[清掃中毒]被治好了
			TALENT:MASTER:清掃中毒 = 0
			MONEY -= FLAG:850 * 10000
			FLAG:850 ++
		ENDIF
	ENDIF
ELSEIF RESULT == 6
	IF GETBIT(CFLAG:72:既成事実,1) && EXP:72:口淫経験 > 10 && !TALENT:72:無接吻経験
		IF TALENT:72:心情 == -1
			PRINTFORMDW %CALLNAME:72%歎息一声把%CALLNAME:MASTER%帯到了裡面
			PRINTFORMDW %CALLNAME:72%露出不高興的樣子拨開頭髮含住了%CALLNAME:MASTER%的陰茎
			PRINTFORMDW 用嘴巴承受了精液的%CALLNAME:72%在洗手漱口後接着診察
			EXP:MASTER:射精経験 ++
			BASE:MASTER:精力 = MAX(BASE:MASTER:精力 - 200, 0)
			EXP:72:口淫経験 ++
			EXP:72:精液経験 ++
			IF TALENT:MASTER:濃厚精液
				PRINTFORMW %CALLNAME:MASTER%的[濃厚精液]被治好了
				TALENT:MASTER:濃厚精液 = 0
			ENDIF
			PRINTL 被要求支付治療費\10000
			MONEY -= 10000
		ELSE
			PRINTFORMDW 因為永琳太忙了所以之後被忽視了…
			TALENT:72:心情 --
		ENDIF
	ELSE
		PRINTFORMDW %CALLNAME:72%説着感謝的話把%CALLNAME:MASTER%帯到了裡面
		PRINTW …
		PRINTW ……
		PRINTW ………
		PRINTFORMDW %CALLNAME:MASTER%は献血に協力することになった…
		BASE:MASTER:体力 = MIN(MAXBASE:MASTER:体力,BASE:MASTER:体力 - 400)
		TALENT:72:心情 = 1
	ENDIF
;ELSEIF RESULT == 10
;	CALL PENIS_CHANGE
ELSEIF RESULT == 100
	RETURN -1
ELSE
	GOTO LOOP
ENDIF
SETBIT TFLAG:一日一回,3
TIME += 30
RETURN 1

@REMEDY(ARG)
SELECTCASE ARG
CASE 0
	LOCALS = 美味的飯
	BASE:MASTER:体力 = MIN(MAXBASE:MASTER:体力,BASE:MASTER:体力 + 500)
CASE 1
	LOCALS = 注射営养剂
	BASE:MASTER:体力 = MIN(MAXBASE:MASTER:体力,BASE:MASTER:体力 + 750)
CASE 2
	LOCALS = 回复薬Ｚ
	BASE:MASTER:体力 = MAXBASE:MASTER:体力
CASE 3
	LOCALS = 季節power
	BASE:MASTER:気力 = MIN(MAXBASE:MASTER:気力,BASE:MASTER:気力 + 500)
CASE 4
	LOCALS = 注射镇静剂
	BASE:MASTER:気力 = MIN(MAXBASE:MASTER:気力,BASE:MASTER:気力 + 750)
CASE 5
	LOCALS = 额头测温
	BASE:MASTER:気力 = MIN(MAXBASE:MASTER:気力,BASE:MASTER:気力 + 900)
CASE 6
	LOCALS = 拥抱
	BASE:MASTER:気力 = MIN(MAXBASE:MASTER:気力,BASE:MASTER:気力 + 1000)
ENDSELECT

PRINTFORMW 永琳进行了%LOCALS%治療
SIF ARG == 2
	PRINTL 干劲満満、状态十足！
SIF ARG >= 0 && ARG <= 2
	PRINTW 体力回復了
SIF ARG >= 3 && ARG <= 6
	PRINTW 気力回復了
[SKIPSTART]
@PENIS_CHANGE
$TINKO_LOOP
PRINTL [1] 因為想重拾童心所以請把小雞雞弄成儿童那样的大小吧
PRINTL [2] 請调整成不会沈溺于放纵中的状态吧
PRINTL [3] 請弄成普通的雞雞
PRINTL [4] 走夜路会害怕請弄成会発光的吧
PRINTL [5] 想让女孩子惊叹所以請往那里面植入珍珠吧
PRINTL [6] 為了能拥有自信請把那里弄大
;PRINTL [7] オークに憧れてるのでドリルみたいにしてください　そしてエルフを探しに行きます
;PRINTL [8] 避妊とかめんどいんで餡餅プカットお願いします
INPUT
SIF TALENT:MASTER:形状 == RESULT
	GOTO TINKO_LOOP
	LOCAL = RESULT
SELECTCASE RESULT
	CASE 1
		IF GETBIT(CFLAG:72:既成事実,1) && CFLAG:72:2 > 5000
			PRINTFORMW 永琳心想竟会有人会提这样的要求呐…
		ELSE
			PRINTFORMW 永琳満头黑线的问你是不是认真的…
	CASE 2
		IF GETBIT(CFLAG:72:既成事実,1) && CFLAG:72:2 > 5000
			PRINTFORMW 永琳勧你改変主意…
		ELSE
	CASE 3
		PRINTFORMW 永琳表示普通就是最好的很快応承下来
	CASE 4
		PRINTFORMW 永琳用像与外星人交流的语气、问你真的想把那地方弄得能発光吗…
	CASE 5
		PRINTFORMW 永琳無話可説
[SKIPEND]

;-------------------------------------------------
;実行可能判定
;-------------------------------------------------
;診察
@COM_ABLE616
;一括管理
SIF GLOBAL_COMABLE(616)
	RETURN RESULT
;永琳が居ないとダメ
SIF CFLAG:MASTER:現在位置 != CFLAG:72:現在位置
	RETURN 0
;デート中
SIF CHK_DATENOW(CFLAG:TARGET:デート中) && TARGET > 0
	RETURN 0
;デート道中
SIF (CFLAG:現在位置 % 100) % 10
	RETURN 0
SIF CFLAG:MASTER:現在位置 != 永遠亭
	RETURN 0
SIF GETBIT(TFLAG:一日一回, 3)
	RETURN 0
;睡眠中
SIF CFLAG:MASTER:睡眠
	RETURN 0
;時間停止中
SIF FLAG:70
	RETURN 0
RETURN 1

