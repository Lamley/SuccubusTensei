;-------------------------------------------------
;64,逆強姦
;-------------------------------------------------
@MESSAGE_COM64
;逆強姦と騎乗位されるは少しニュアンスが違う気がするけど、需要がはっきりするまで保留します
;;連続
;IF SELECTCOM == PREVCOM && ASSIPLAY == TFLAG:150
;	PRINTFORML %CALLNAME:PLAYER%は%CALLNAME:TARGET%の%TINKO(PLAYER)%をきつく食い締めたまま、執拗に腰を振りたてた…
;ELSE
;	PRINTFORM %CALLNAME:PLAYER%%TINKO(PLAYER)%を強引に、
;	;調教者が処女
;	SIF TALENT:PLAYER:処女 == 1
;		PRINT 男を受け入れた事のない
;	PRINTL ヴァギナの奥まで咥えこんだ…
;ENDIF

;抜かず
IF TCVAR:PLAYER:100 == TARGET && TARGET == TFLAG:103
	;対象が射精
	IF NOWEX:TARGET:11
		PRINTFORML %CALLNAME:TARGET%騎在%CALLNAME:PLAYER%身上、
			PRINTFORML 順从着上涌的衝動向上耸動起了腰部
		;調教者も射精
		SIF NOWEX:PLAYER:11
			PRINTL  
			PRINTFORM 同時高潮的%CALLNAME:PLAYER%的%TINKO(PLAYER)%也剧烈震動着、喷出了灼熱的白濁
		PRINTFORML …
	;前ターンでＶ中出し
	ELSEIF TCVAR:PLAYER:102
		PRINTFORML %CALLNAME:TARGET%在%CALLNAME:PLAYER%的最深処注入了精液、為了不讓発熱的内部平静下来継續動着腰
		PRINTFORML 沈浸在膣内射精的余韻中的%CALLNAME:PLAYER%雖然因為毫不停息的動作而有些動搖、但還是継續搖動着身体…
	ELSE
		PRINTFORML %CALLNAME:TARGET%沒有從膣内抜出肉棒就直接抱起了%CALLNAME:PLAYER%、從下面往上頂着腰…
	ENDIF
ELSE
	IF TALENT:PLAYER:処女 == 1
		;逆強姦は主人公主導の主人公穴相手竿と考え修正
		;PRINTFORM %CALLNAME:TARGET%骑在%CALLNAME:PLAYER%身上、慢慢的沈下腰
		PRINTFORM %CALLNAME:PLAYER%跨坐在%CALLNAME:TARGET%身上、慢慢地沈下了腰
		;処女＋矮小体型
		IF TALENT:PLAYER:体型 < 0
			PRINTFORML 、用没尝过男人滋味的幼小裂缝呑下了%TINKO(PLAYER)%…
		;処女
		ELSE
			PRINTFORML 、用没尝过男人滋味的小穴呑下了%TINKO(PLAYER)%…
		ENDIF
	ELSE
		;逆強姦は主人公主導の主人公穴相手竿と考え修正
		;PRINTFORM %CALLNAME:TARGET%%CALLNAME:PLAYER%の上にまたがって、
		PRINTFORM %CALLNAME:PLAYER%跨坐在%CALLNAME:TARGET%身上、
		SIF TEQUIP:PLAYER:0 & 2
			PRINTFORM 通过%SHOW_BOTTOM(PLAYER,2)%的間隙
		PRINTFORML 用%SLIT()%把%TINKO(TARGET)%呑了下去…
		PRINTFORML 
		;ちんちん
		IF TALENT:TARGET:2 & 2
			PRINTFORM 散発着热气的%CALLNAME:TARGET%的%TINKO(TARGET)%
		;にせちん
		ELSE
			PRINTFORM 無機質的穿戴式假陰茎
		ENDIF
		PRINTFORML 貫穿全身的感覚、讓%CALLNAME:PLAYER%喘息不已…
	ENDIF
ENDIF
;-------------------------------------------------
;66,騎乗位肛交
;-------------------------------------------------
@MESSAGE_COM66
IF FLAG:70 == 1
	;連続
	IF SELECTCOM == PREVCOM && ASSIPLAY == TFLAG:150
		PRINTFORML 让時停中的%CALLNAME:TARGET%保持骑在自己身上的姿势、再次開始向上耸動起了腰…
	;抜かず
	ELSEIF TCVAR:101 == PLAYER
		PRINTFORML 没有从腸内抜出并抱起身体无法動弾的%CALLNAME:TARGET%、從下面往上頂着腰…
	ELSE
		PRINTFORM %CALLNAME:PLAYER%分開時停中的%CALLNAME:TARGET%的腿、钻到了%CALLNAME:TARGET%身下対着
		;潤滑LV4以上
		SIF PALAM:潤滑 >= PALAMLV:4
			PRINT 充満粘液的
		PRINTFORML 肛门慢慢的将%CALLNAME:PLAYER%的阳物插了进去…
	ENDIF
ELSE
	;連続
	IF SELECTCOM == PREVCOM && ASSIPLAY == TFLAG:150
		PRINTFORML %CALLNAME:TARGET%在被挿入的情况下紧紧缠住阳物、再次開始動起了腰…
	;抜かず
	ELSEIF TCVAR:101 == PLAYER
		PRINTFORML 没有从腸内抜出并抱起了%CALLNAME:TARGET%、從下面往上頂着腰…
	ELSE
		PRINTFORM %CALLNAME:TARGET%骑在%CALLNAME:PLAYER%身上后、用
		;潤滑LV4以上
		SIF PALAM:潤滑 >= PALAMLV:4
			PRINT 充満粘液的
		PRINTFORML 肛门慢慢的将%CALLNAME:PLAYER%的阳物呑了下去…
	ENDIF
	;陥落済み
	IF TALENT:恋慕
		PRINTFORM %CALLNAME:TARGET%
		;矮小体型
		SIF TALENT:体型 < 0
			PRINT 用小小的身体拼命地
		;巨乳
		SIF TALENT:胸囲 == 1
			PRINT 大大的乳房摇晃着的同时
		;爆乳
		SIF TALENT:胸囲 == 2
			PRINT 跳動的胸部令身体随之晃動的同时
		PRINTL 自己動起了腰…
	ENDIF
ENDIF
;-------------------------------------------------
;71,二穴挿入
;-------------------------------------------------
@MESSAGE_COM71
VARSET LOCAL
LOCAL:1 = MASTER_POSE(SET_V,SET_P,1)
LOCAL:2 = MASTER_POSE(SET_A,SET_P,1)
IF LOCAL:1
	LOCAL = LOCAL:1
ELSEIF LOCAL:2
	LOCAL = LOCAL:2
ENDIF
;LOCAL 二穴挿入される人
;連続
IF PREVCOM == SELECTCOM
	PRINTFORML %CALLNAME:LOCAL%前后被同时插入、忘我地在暴力的快感下娇喘不已…
	RETURN 0
;主人W挿し
ELSEIF ((LOCAL:1 == 0 || LOCAL:2 == 0) && LOCAL == TARGET) && TALENT:MASTER:双槍
	PRINTFORML %CALLNAME:MASTER%用两根陰茎贯穿了%CALLNAME:LOCAL%的双穴…
;主人V挿入-TARGET A挿入
ELSEIF LOCAL == LOCAL:1
	PRINTFORML %CALLNAME:MASTER%保持着连接状态掰開了%CALLNAME:LOCAL%的屁股、催促%CALLNAME:TARGET%也来侵犯後穴…
;主人A挿入-助手V挿入
ELSE
	PRINTFORML %CALLNAME:MASTER%在贯穿肛门的状態下抱起%CALLNAME:LOCAL%、分開两腿催促%CALLNAME:TARGET%也来侵犯前穴…
ENDIF

[SKIPSTART]
;-------------------------------------------------
;74,圓周運動
;-------------------------------------------------
@MESSAGE_COM75
IF ABL:MASTER:腰 < 3
	PRINTFORML %CALLNAME:MASTER%用腰顶住%CALLNAME:TARGET%笨拙地開始左右运動了起来。
ELSE
	PRINTFORML %CALLNAME:MASTER%用腰顶住%CALLNAME:TARGET%慢慢的一圈一圈的動起了腰。
ENDIF
;-------------------------------------------------
;75,讓做圓周運動
;-------------------------------------------------
@MESSAGE_COM75
被%CALLNAME:MASTER%命令道自己動的%CALLNAME:TARGET%、満臉通紅的開始慢慢地動起了腰。
被%CALLNAME:MASTER%命令道自己動的%CALLNAME:TARGET%、臉上泛起紅潮張開衣服開始晃動着腰。
%CALLNAME:MASTER%都不用説%CALLNAME:TARGET%就開始用笨拙的動作前後動起了腰。
%CALLNAME:MASTER%一停下腰部的動作%CALLNAME:TARGET%立刻就開始前後晃動着腰。
%CALLNAME:TARGET%貪求着快感、即使%CALLNAME:MASTER%沒有命令也開始大幅度動起了腰。
%CALLNAME:MASTER%懇求着%CALLNAME:TARGET%動起来。%CALLNAME:TARGET%露出嗜虐的笑容開始晃起了腰。


（搖動腰部中。）
%CALLNAME:TARGET%沈浸在晃動腰部所得到的快感之中。
%CALLNAME:TARGET%無法隠藏住快要融化的表情貪求着更多的快感而大幅度晃動着腰。
是因為理解了怎麼動作才能摩擦到自己最有感覚的地方吧、%CALLNAME:TARGET%動作的時候泛紅的臉上慢慢地露出了高潮臉。
%CALLNAME:TARGET%完全沒有在意流出的口水和泪水、像野獣一樣一味地持續晃動着腰。
%CALLNAME:TARGET%用失去焦点的眼睛凝視着虚空、機械式的晃動着腰。
%CALLNAME:TARGET%一辺凝視着%CALLNAME:MASTER%的臉一辺晃動着腰。比起自己的快楽来説更想讓%CALLNAME:MASTER%獲得満足。
%CALLNAME:TARGET%不知道該怎麼動作才好、帯着困惑用笨拙的動作晃動着腰。
[SKIPEND]
;-------------------------------------------------
;79,小人飛機杯
;-------------------------------------------------
@MESSAGE_COM79
IF TCVAR:100 == PLAYER && TARGET == TFLAG:103
	IF TALENT:PLAYER:2 & 2
		PRINTFORMW 在品味着%CALLNAME:TARGET%膣内触感的同时、再次開始用%CALLNAME:TARGET%撸起了陰茎……
	;穿戴式假陰茎を使用している場合
	ELSE
		PRINTFORMW 没有从%CALLNAME:TARGET%的膣内抜出、再次開始用%CALLNAME:TARGET%撸起了陰茎……
	ENDIF
	SIF !FLAG:70 && EXP:Ｖ拡張経験 < 25 && !CFLAG:MASTER:イタズラ == 2
		PRINTFORML %CALLNAME:TARGET%翻着白眼発出了没有含义的呻吟…
ELSE
	PRINTFORM 用力抓住%CALLNAME:TARGET%、往那
	;処女
	IF TALENT:0
		PRINT 不识男人滋味的
	;潤滑LV4以上
	ELSEIF PALAM:4 >= PALAMLV:4
		PRINT 充分湿润的
	ELSEIF PALAM:4 <= PALAMLV:2
		PRINT 还没充分湿润的
	ENDIF
	IF EXP:Ｖ拡張経験 < 5
		PRINTFORML 小小的细缝中将%TINKO(PLAYER)%用力插了进去
	ELSE
		PRINTL 小小的细缝貫穿了进去
	ENDIF
	IF FLAG:70 || CFLAG:MASTER:イタズラ == 2
		IF EXP:Ｖ拡張経験 < 10
			PRINTFORM 毫不在意从%CALLNAME:TARGET%的小穴中発出的撕裂般的讨厌声音、
		ELSEIF EXP:Ｖ拡張経験 > 50
			PRINTFORM 巨大的陰茎被軽易地呑没进去
		ENDIF
	ELSE
		IF ABL:受虐属性 > 3 && EXP:Ｖ拡張経験 > 50
			PRINT 像飛機杯一般使用被巨大的肉槍串刺而露出恍惚表情的
		ELSEIF ABL:受虐属性 > 2 && EXP:Ｖ拡張経験 > 25
			PRINT 像飛機杯一般使用大张着嘴脸颊涨红的
		ELSE
			PRINT 像飛機杯一般使用被巨大的陰茎圧迫内臓発出苦悶的尖叫的
		ENDIF
	ENDIF
	PRINTFORML %CALLNAME:TARGET%的身体撸着陰茎……
	PRINTFORML %CALLNAME:TARGET%的肚子上诡异地浮现出了%CALLNAME:PLAYER%陰茎的形状、与%CALLNAME:TARGET%小小的身体形成了鲜明対比……
ENDIF
;-------------------------------------------------
;92,被Ａ正常位
;-------------------------------------------------
@MESSAGE_COM92
;被Ａ対面立位
IF (!BEDROOM(CFLAG:PLAYER:現在位置) && !(BATHROOM() && ITEM:20))
	;抜かずに
	IF TCVAR:PLAYER:101 == TARGET && TARGET == TFLAG:103
		;対象が射精
		IF NOWEX:TARGET:11
			PRINTFORML %CALLNAME:PLAYER%被%CALLNAME:TARGET%抱在懷中、侵犯着肛腔
			PRINTFORML %CALLNAME:TARGET%渾身激烈顫抖的同時、往%CALLNAME:PLAYER%的直腸内注入了%SAMEN_DESCRIPTION(TARGET)%
			;調教者も射精。あへぇ
			SIF NOWEX:PLAYER:11
				PRINTFORM 同時高潮的%CALLNAME:PLAYER%的%TINKO(PLAYER)%也剧烈震動着、喷出了灼熱的%SAMEN_DESCRIPTION(MASTER)%
			PRINTFORML …
		;前ターンでＡ中出しされてる
		ELSEIF TCVAR:PLAYER:102
			PRINTFORML %CALLNAME:TARGET%晃動着腰、貫穿了%CALLNAME:PLAYER%的菊座
			PRINTFORM %SAMEN_DESCRIPTION(TARGET)%和
			IF TALENT:TARGET:2 & 2
				PRINTFORM %TINKO(TARGET)%
			;にせちん（てかチンコないのに中出しされてるってどういうこと？
			ELSE
				PRINTFORM 穿戴式假陰茎
			ENDIF
			PRINTFORML 交纏在一起、在尻穴中響起了卑猥的粘着音
		ELSE
			PRINTFORML 被%TINKO(TARGET)%蹂躙着腸壁的%CALLNAME:PLAYER%的腰部向前挪動
			PRINTFORML %CALLNAME:TARGET%開始動起腰侵犯着全身無力的%CALLNAME:PLAYER%…
		ENDIF
	ELSE
		PRINTFORML 被%CALLNAME:TARGET%\@ ABL:施虐属性 >= 3 ? 強行 # 温柔地 \@抱在懷中
		PRINTFORM %CALLNAME:PLAYER%用一隻腿纏住了%CALLNAME:TARGET%的腰、
		IF EXP:PLAYER:Ａ経験 >= EXPLV:4
			PRINTFORM 用柔軟的肛門、像在軽咬一般把
			IF TALENT:TARGET:2 & 2
				PRINTFORM %TINKO(TARGET)%
			;にせちん
			ELSE
				PRINTFORM 穿戴式假陰茎
			ENDIF
			PRINTFORML 呑了進去
		ELSEIF EXP:PLAYER:Ａ経験 >= EXPLV:3
			PRINTFORM 慢慢地讓
			IF TALENT:TARGET:2 & 2
				PRINTFORM %TINKO(TARGET)%
			;にせちん
			ELSE
				PRINTFORM 穿戴式假陰茎
			ENDIF
			PRINTFORML 擠進了肛門之中
		ELSE
			PRINTFORML 因為還未習慣異物的肛門被貫穿的痛苦、漏出了呻吟
		ENDIF

		;ちんちん
		IF TALENT:TARGET:2 & 2
			PRINTFORM 散発着热气的%CALLNAME:TARGET%的%TINKO(TARGET)%
		;にせちん
		ELSE
			PRINTFORM 無機質的穿戴式假陰茎
		ENDIF
		PRINTFORML 剜動肛門玩弄腸壁的感覚、讓%CALLNAME:PLAYER%喘息不已…
	ENDIF
;被Ａ正常位
ELSE
	;抜かずに
	IF TCVAR:PLAYER:101 == TARGET && TARGET == TFLAG:103
		;対象が射精
		IF NOWEX:TARGET:11
			PRINTFORML %CALLNAME:PLAYER%被%CALLNAME:TARGET%圧在身上、侵犯着肛腔
			PRINTFORML %CALLNAME:TARGET%渾身激烈顫抖的同時、往%CALLNAME:PLAYER%的直腸内注入了%SAMEN_DESCRIPTION(TARGET)%
			;調教者も射精。あへぇ
			SIF NOWEX:PLAYER:11
				PRINTFORM 同時高潮的%CALLNAME:PLAYER%的%TINKO(PLAYER)%也剧烈震動着、喷出了灼熱的%SAMEN_DESCRIPTION(MASTER)%
			PRINTFORML …
		;前ターンでＡ中出し
		ELSEIF TCVAR:PLAYER:102
			PRINTFORML %CALLNAME:TARGET%晃動着腰、貫穿了%CALLNAME:PLAYER%的菊座
			PRINTFORM %SAMEN_DESCRIPTION(TARGET)%和
			IF TALENT:TARGET:2 & 2
				PRINTFORM %TINKO(TARGET)%
			;にせちん
			ELSE
				PRINTFORM 穿戴式假陰茎
			ENDIF
			PRINTFORML 交纏在一起、在尻穴中響起了卑猥的粘着音
		ELSE
			PRINTFORML 讓被%TINKO(TARGET)%剜弄着腸壁的%CALLNAME:PLAYER%仰面躺下
			PRINTFORML %CALLNAME:TARGET%開始動起腰侵犯着全身無力的%CALLNAME:PLAYER%…
		ENDIF
	ELSE
		PRINTFORM 
		IF ABL:施虐属性 >= 3
			PRINTFORM 被%CALLNAME:TARGET%粗暴地推倒仰面躺下的%CALLNAME:PLAYER%、
		ELSE
			PRINTFORM 在%CALLNAME:TARGET%要求下仰面躺下的%CALLNAME:PLAYER%、
		ENDIF
		PRINTFORML 把腰抬到了自己頭頂
		PRINTFORM 自己掰開臀部的%CALLNAME:PLAYER%、
		IF EXP:PLAYER:Ａ経験 >= EXPLV:4
			PRINTFORM 帯着陶酔的表情用柔軟的肛門、像在軽咬一般把%CALLNAME:TARGET%的
			IF TALENT:TARGET:2 & 2
				PRINTFORM %TINKO(TARGET)%
			;にせちん
			ELSE
				PRINTFORM 穿戴式假陰茎
			ENDIF
			PRINTFORML 呑了進去
		ELSEIF EXP:PLAYER:Ａ経験 >= EXPLV:3
			PRINTFORM 慢慢地讓
			IF TALENT:TARGET:2 & 2
				PRINTFORM %TINKO(TARGET)%
			;にせちん
			ELSE
				PRINTFORM 穿戴式假陰茎
			ENDIF
			PRINTFORML 插入了自己已経張開的肛門
		ELSE
			PRINTFORML 因為完全不习惯異物的肛門被插入、漏出了苦鳴
		ENDIF

		;ちんちん
		IF TALENT:TARGET:2 & 2
			PRINTFORM 散発着热气的%CALLNAME:TARGET%的%TINKO(TARGET)%
		;にせちん
		ELSE
			PRINTFORM 無機質的穿戴式假陰茎
		ENDIF
		PRINTFORML 侵犯腸壁的感覚、讓%CALLNAME:PLAYER%喘息不已…
	ENDIF
ENDIF

;-------------------------------------------------
;93,被Ａ後背位
;-------------------------------------------------
@MESSAGE_COM93
;被Ａ背面立位
IF (!INROOM(CFLAG:PLAYER:現在位置) && !(BATHROOM() && ITEM:20))
	;抜かずに
	IF TCVAR:PLAYER:101 == TARGET && TARGET == TFLAG:103
		;対象が射精
		IF NOWEX:TARGET:11
			PRINTFORML 被腰部撞撃着臀肉的%CALLNAME:PLAYER%、收縮着菊穴緊緊夾住了%CALLNAME:TARGET%插入深処的陽具
			PRINTFORM 從顫抖着的肉棒射入到直腸内的%SAMEN_DESCRIPTION(TARGET)%、讓%CALLNAME:PLAYER%発出了灼熱的吐息
			;調教者も射精
			IF NOWEX:PLAYER:11
							PRINTL 
				PRINTFORM 從同時高潮的%CALLNAME:PLAYER%的%TINKO(PLAYER)%射出了灼熱的%SAMEN_DESCRIPTION(MASTER)%、弄髒了地板
			ENDIF
			PRINTL …
		;前ターンでＡ中出し
		ELSEIF TCVAR:PLAYER:102
			PRINTFORML %CALLNAME:PLAYER%還在被%CALLNAME:TARGET%持續蹂躙着菊座
			PRINTFORM 塗満了精液和腸液的
			IF TALENT:TARGET:2 & 2
				PRINTFORM %TINKO(TARGET)%
			ELSE
				PRINTFORM 穿戴式假陰茎
			ENDIF
			PRINTFORML 的動作越来越快、表達了%CALLNAME:TARGET%的昂奮…
		ELSE
			PRINTFORML %CALLNAME:PLAYER%按照%CALLNAME:TARGET%所説\@ OPENPLACE(CFLAG:MASTER:現在位置) == 2 ? 朝後伸出双手 # 双手扶在墻上 \@、因為肛門被抽插的快感而喘息不已…
		ENDIF
	ELSE
		PRINTFORM 対%CALLNAME:TARGET%発出的\@ OPENPLACE(CFLAG:MASTER:現在位置) == 2 ? 朝後伸出双手 # 双手扶在墻上 \@的
		IF ABL:施虐属性 >= 3
			PRINTFORM 命令、
		ELSE
			PRINTFORM 請求、
		ENDIF
		PRINTFORML %CALLNAME:PLAYER%老実的遵從
		SIF TEQUIP:PLAYER:0 & 2
			PRINTFORM 通过%SHOW_BOTTOM(PLAYER,2)%的間隙
		IF EXP:PLAYER:Ａ経験 >= EXPLV:4
			PRINTFORM 帯着陶酔的表情用柔軟的肛門、像在軽咬一般把%CALLNAME:TARGET%的
			IF TALENT:TARGET:2 & 2
				PRINTFORM %TINKO(TARGET)%
			;にせちん
			ELSE
				PRINTFORM 穿戴式假陰茎
			ENDIF
			PRINTFORML 呑了進去
		ELSEIF EXP:PLAYER:Ａ経験 >= EXPLV:3
			PRINTFORM 慢慢地讓
			IF TALENT:TARGET:2 & 2
				PRINTFORM %TINKO(TARGET)%
			;にせちん
			ELSE
				PRINTFORM 穿戴式假陰茎
			ENDIF
			PRINTFORML 插入了自己已経張開的肛門
		ELSE
			PRINTFORML 完全不习惯異物的肛門被強行插入、讓%CALLNAME:MASTER%漏出了苦鳴
		ENDIF
		PRINTFORML 
		;ちんちん
		IF TALENT:TARGET:2 & 2
			PRINTFORM 散発着热气的%CALLNAME:TARGET%的%TINKO(TARGET)%
		;にせちん
		ELSE
			PRINTFORM 無機質的穿戴式假陰茎
		ENDIF
		PRINTFORML 激烈攪弄着膣襞的感覚、讓%CALLNAME:PLAYER%喘息不已…
	ENDIF
;被Ａ後背位
ELSE
	;抜かずに
	IF TCVAR:PLAYER:101 == TARGET && TARGET == TFLAG:103
		;対象が射精
		IF NOWEX:TARGET:11
			PRINTFORML 被腰部撞撃着臀肉的%CALLNAME:PLAYER%、收縮着菊穴緊緊夾住了%CALLNAME:TARGET%插入深処的陽具
			PRINTFORM 從顫抖着的肉棒射入到直腸内的%SAMEN_DESCRIPTION(TARGET)%、讓%CALLNAME:PLAYER%発出了灼熱的吐息
			;調教者も射精
			IF NOWEX:PLAYER:11
							PRINTL 
				PRINTFORM 從同時高潮的%CALLNAME:PLAYER%的%TINKO(PLAYER)%射出了灼熱的%SAMEN_DESCRIPTION(MASTER)%、弄髒了地板
			ENDIF
			PRINTL …
		;前ターンでＡ中出し
		ELSEIF TCVAR:PLAYER:102
			PRINTFORML %CALLNAME:PLAYER%還在被%CALLNAME:TARGET%持續蹂躙着菊座
			PRINTFORM 塗満了精液和腸液的
			IF TALENT:TARGET:2 & 2
				PRINTFORM %TINKO(TARGET)%
			ELSE
				PRINTFORM 穿戴式假陰茎
			ENDIF
			PRINTFORML 的動作越来越快、表達了%CALLNAME:TARGET%的昂奮…
		ELSE
			PRINTFORML %CALLNAME:PLAYER%按照%CALLNAME:TARGET%所説四肢着地趴在地上、因為肛門被抽插的快感而喘息不已…
		ENDIF
	ELSE
		PRINTFORM 面対%CALLNAME:TARGET%傳達的四肢着地趴在地上的
		IF ABL:施虐属性 >= 3
			PRINTFORM 命令、
		ELSE
			PRINTFORM 請求、
		ENDIF
		PRINT
		PRINTFORML %CALLNAME:PLAYER%老実的遵從
		SIF TEQUIP:PLAYER:0 & 2
			PRINTFORM 通过%SHOW_BOTTOM(PLAYER,2)%的間隙
		IF EXP:PLAYER:Ａ経験 >= EXPLV:4
			PRINTFORM 帯着陶酔的表情用柔軟的肛門、像在軽咬一般把%CALLNAME:TARGET%的
			IF TALENT:TARGET:2 & 2
				PRINTFORM %TINKO(TARGET)%
			;にせちん
			ELSE
				PRINTFORM 穿戴式假陰茎
			ENDIF
			PRINTFORML 呑了進去
		ELSEIF EXP:PLAYER:Ａ経験 >= EXPLV:3
			PRINTFORM 慢慢地讓
			IF TALENT:TARGET:2 & 2
				PRINTFORM %TINKO(TARGET)%
			;にせちん
			ELSE
				PRINTFORM 穿戴式假陰茎
			ENDIF
			PRINTFORML 插入了自己已経張開的肛門
		ELSE
			PRINTFORML 完全不习惯異物的肛門被強行插入、讓%CALLNAME:MASTER%漏出了苦鳴
		ENDIF
		PRINTFORML 
		;ちんちん
		IF TALENT:TARGET:2 & 2
			PRINTFORM 散発着热气的%CALLNAME:TARGET%的%TINKO(TARGET)%
		;にせちん
		ELSE
			PRINTFORM 無機質的穿戴式假陰茎
		ENDIF
		PRINTFORML 激烈攪弄着膣襞的感覚、讓%CALLNAME:PLAYER%喘息不已…
	ENDIF
ENDIF

;-------------------------------------------------
;130,被正常位
;-------------------------------------------------
@MESSAGE_COM130
;被対面立位
IF (!BEDROOM(CFLAG:PLAYER:現在位置) && !(BATHROOM() && ITEM:20))
	;抜かず
	IF TCVAR:PLAYER:100 == TARGET && TARGET == TFLAG:103
		;対象が射精
		IF NOWEX:TARGET:11
			PRINTFORML %CALLNAME:PLAYER%被%CALLNAME:TARGET%緊緊抱住、侵犯着膣腔
			PRINTFORML %CALLNAME:TARGET%渾身激烈顫抖的同時、在%CALLNAME:PLAYER%的子宮内注入了%SAMEN_DESCRIPTION(TARGET)%
			;調教者も射精。あへぇ
			SIF NOWEX:PLAYER:11
				PRINTFORM 同時高潮的%CALLNAME:PLAYER%的%TINKO(PLAYER)%也剧烈震動着、喷出了灼熱的%SAMEN_DESCRIPTION(MASTER)%
			PRINTFORML …
		;前ターンでＶ中出し
		ELSEIF TCVAR:PLAYER:102
			PRINTFORML %CALLNAME:TARGET%動着腰、把精液塗満了%CALLNAME:PLAYER%的膣襞
			PRINTFORML 從被%SAMEN_DESCRIPTION(TARGET)%侵犯的胎内、%CALLNAME:PLAYER%能聽見下流的声音在不断回響…
		ELSE
			PRINTFORML %TINKO(TARGET)%に膣襞をこすられながら%CALLNAME:PLAYER%は片足と両腕を%CALLNAME:TARGET%に巻き付けた
			PRINTFORML ぎゅっとしがみつく%CALLNAME:PLAYER%に、%CALLNAME:TARGET%の腰が動き始める…
		ENDIF
	ELSE
		PRINTFORML 被%CALLNAME:TARGET%\@ ABL:施虐属性 >= 3 ? 強行 #  \@抱在懷中、 %CALLNAME:PLAYER%は自ら腰を突き出し%TINKO(TARGET)%に膣口をこすりつける
		PRINTFORM %CALLNAME:TARGET%\@ ABL:施虐属性 >= 3 ? 冷 # 好像很高興的 \@笑着、
		SIF TEQUIP:PLAYER:0 & 2
			PRINTFORM 通过%SHOW_BOTTOM(PLAYER,2)%的間隙
		PRINTFORM 把
		IF TALENT:TARGET:2 & 2
			PRINTFORM %TINKO(TARGET)%
		;にせちん
		ELSE
			PRINTFORM 穿戴式假陰茎
		ENDIF
		PRINTFORML 一口気全插入了%SLIT()%…
		PRINTFORML 
		;ちんちん
		IF TALENT:TARGET:2 & 2
			PRINTFORM 散発着热气的%CALLNAME:TARGET%的%TINKO(TARGET)%
		;にせちん
		ELSE
			PRINTFORM 無機質的穿戴式假陰茎
		ENDIF
		PRINTFORML 激烈攪弄着膣襞的感覚、讓%CALLNAME:PLAYER%喘息不已…
	ENDIF
;被正常位
ELSE
	;抜かず
	IF TCVAR:PLAYER:100 == TARGET && TARGET == TFLAG:103
		;対象が射精
		IF NOWEX:TARGET:11
			PRINTFORML %CALLNAME:PLAYER%被%CALLNAME:TARGET%圧在身上、剜動着膣腔
			PRINTFORML %CALLNAME:TARGET%渾身激烈顫抖的同時、在%CALLNAME:PLAYER%的子宮内注入了%SAMEN_DESCRIPTION(TARGET)%
			;調教者も射精。あへぇ
			SIF NOWEX:PLAYER:11
				PRINTFORM 同時高潮的%CALLNAME:PLAYER%的%TINKO(PLAYER)%也剧烈震動着、喷出了灼熱的%SAMEN_DESCRIPTION(MASTER)%
			PRINTFORML …
		;前ターンでＶ中出し
		ELSEIF TCVAR:PLAYER:102
			PRINTFORML %CALLNAME:TARGET%動着腰、把精液塗満了%CALLNAME:PLAYER%的膣襞
			PRINTFORML 從被%SAMEN_DESCRIPTION(TARGET)%侵犯的胎内、%CALLNAME:PLAYER%能聽見下流的声音在不断回響…
		ELSE
			PRINTFORML 讓被%TINKO(TARGET)%摩擦着膣襞的%CALLNAME:PLAYER%仰面躺下
			PRINTFORML %CALLNAME:TARGET%開始動起腰侵犯着全身無力的%CALLNAME:PLAYER%…
		ENDIF
	ELSE
		PRINTFORM 
		IF ABL:施虐属性 >= 3
			PRINTFORM 被%CALLNAME:TARGET%粗暴地推倒仰面躺下的%CALLNAME:PLAYER%、
		ELSE
			PRINTFORM 在%CALLNAME:TARGET%要求下仰面躺下的%CALLNAME:PLAYER%、
		ENDIF
		PRINTFORML %CALLNAME:PLAYER%用僅剩的力気張開双腿、親手掰開了自己的陰唇
		PRINTFORM 凝視着暴露的私処、不由得呑了口口水的%CALLNAME:TARGET%用
		SIF TEQUIP:PLAYER:0 & 2
			PRINTFORM 通过%SHOW_BOTTOM(PLAYER,2)%的間隙
		PRINTFORM 把
		IF TALENT:TARGET:2 & 2
			PRINTFORM %TINKO(TARGET)%
		;にせちん
		ELSE
			PRINTFORM 穿戴式假陰茎
		ENDIF
		PRINTFORML 直接挿入了%SLIT()%…
		PRINTFORML 
		;ちんちん
		IF TALENT:TARGET:2 & 2
			PRINTFORM 散発着热气的%CALLNAME:TARGET%的%TINKO(TARGET)%
		;にせちん
		ELSE
			PRINTFORM 無機質的穿戴式假陰茎
		ENDIF
		PRINTFORML 激烈攪弄着膣襞的感覚、讓%CALLNAME:PLAYER%喘息不已…
	ENDIF
ENDIF

;-------------------------------------------------
;131,被後背位
;-------------------------------------------------
@MESSAGE_COM131
;被背面立位
IF (!INROOM(CFLAG:PLAYER:現在位置) && !(BATHROOM() && ITEM:20))
	;抜かず
	IF TCVAR:PLAYER:100 == TARGET && TARGET == TFLAG:103
		;対象が射精
		IF NOWEX:TARGET:11
			PRINTFORML 被反復揉搓着臀肉的%CALLNAME:PLAYER%、用力地挺起臀部讓%TINKO(TARGET)%的亀頭頂在子宮口之上
			PRINTFORML 從顫抖着的肉棒射入到子宮内的%SAMEN_DESCRIPTION(TARGET)%、讓%CALLNAME:PLAYER%発出了灼熱的吐息
			;調教者も射精
			IF NOWEX:PLAYER:11
							PRINTL 
				PRINTFORM 從同時高潮的%CALLNAME:PLAYER%的%TINKO(PLAYER)%射出了灼熱的%SAMEN_DESCRIPTION(MASTER)%、弄髒了地板
			ENDIF
			PRINTL …
		;前ターンでＶ中出し
		ELSEIF TCVAR:PLAYER:102
			PRINTFORML %CALLNAME:PLAYER%還在被%CALLNAME:TARGET%持續蹂躙着膣肉
			PRINTFORML %CALLNAME:PLAYER%在被塗満精液和愛蜜的膣襞傳来的快感下任憑擺佈
			PRINTFORM 塗満了男女的愛液的
			IF TALENT:TARGET:2 & 2
				PRINTFORM %TINKO(TARGET)%
			ELSE
				PRINTFORM 穿戴式假陰茎
			ENDIF
			PRINTFORML 的動作越来越快、表達了%CALLNAME:TARGET%的昂奮…
		ELSE
			PRINTFORML %CALLNAME:PLAYER%按照%CALLNAME:TARGET%所説\@ OPENPLACE(CFLAG:MASTER:現在位置) == 2 ? 朝後伸出双手 # 双手扶在墻上 \@、因為膣腔被抽插的快感而喘息不已…
		ENDIF
	ELSE
		PRINTFORM 対%CALLNAME:TARGET%発出的\@ OPENPLACE(CFLAG:MASTER:現在位置) == 2 ? 朝後伸出双手 # 双手扶在墻上 \@的
		IF ABL:施虐属性 >= 3
			PRINTFORM 命令、
		ELSE
			PRINTFORM 請求、
		ENDIF
		PRINTFORML %CALLNAME:PLAYER%老実的遵從
		SIF TEQUIP:PLAYER:0 & 2
			PRINTFORM 通过%SHOW_BOTTOM(PLAYER,2)%的間隙
		PRINTFORML 把
		IF TALENT:TARGET:2 & 2
			PRINTFORM %TINKO(TARGET)%
		;にせちん
		ELSE
			PRINTFORM 穿戴式假陰茎
		ENDIF
		PRINTFORML 直接挿入了%SLIT()%的最深処…
		PRINTFORML 
		;ちんちん
		IF TALENT:TARGET:2 & 2
			PRINTFORM 散発着热气的%CALLNAME:TARGET%的%TINKO(TARGET)%
		;にせちん
		ELSE
			PRINTFORM 無機質的穿戴式假陰茎
		ENDIF
		PRINTFORML 激烈攪弄着膣襞的感覚、讓%CALLNAME:PLAYER%喘息不已…
	ENDIF
;被後背位
ELSE
	;抜かず
	IF TCVAR:PLAYER:100 == TARGET && TARGET == TFLAG:103
		;対象が射精
		IF NOWEX:TARGET:11
			PRINTFORML 被反復揉搓着臀肉的%CALLNAME:PLAYER%、用女陰含住了陽具親自扭動着腰
			PRINTFORML 從顫抖着的肉棒射入到子宮内的%SAMEN_DESCRIPTION(TARGET)%、讓%CALLNAME:PLAYER%発出了灼熱的吐息
			;調教者も射精
			IF NOWEX:PLAYER:11
							PRINTL 
				PRINTFORM 從同時高潮的%CALLNAME:PLAYER%的%TINKO(PLAYER)%射出了灼熱的%SAMEN_DESCRIPTION(MASTER)%、弄髒了地板
			ENDIF
			PRINTL …
		;前ターンでＶ中出し
		ELSEIF TCVAR:PLAYER:102
			PRINTFORML %CALLNAME:PLAYER%還在被%CALLNAME:TARGET%持續蹂躙着膣肉
			PRINTFORML %CALLNAME:PLAYER%在被塗満精液和愛蜜的膣襞傳来的快感下任憑擺佈
			PRINTFORM 塗満了男女的愛液的
			IF TALENT:TARGET:2 & 2
				PRINTFORM %TINKO(TARGET)%
			ELSE
				PRINTFORM 穿戴式假陰茎
			ENDIF
			PRINTFORML 的動作越来越快、表達了%CALLNAME:TARGET%的昂奮…
		ELSE
			PRINTFORML %CALLNAME:PLAYER%按照%CALLNAME:TARGET%所説四肢着地趴在地上、因為膣腔被抽插的快感而喘息不已…
		ENDIF
	ELSE
		PRINTFORM 面対%CALLNAME:TARGET%傳達的四肢着地趴在地上的
		IF ABL:施虐属性 >= 3
			PRINTFORM 命令、
		ELSE
			PRINTFORM 請求、
		ENDIF
		PRINT
		PRINTFORML %CALLNAME:PLAYER%老実的遵從
		SIF TEQUIP:PLAYER:0 & 2
			PRINTFORM 通过%SHOW_BOTTOM(PLAYER,2)%的間隙
		PRINTFORML 把
		IF TALENT:TARGET:2 & 2
			PRINTFORM %TINKO(TARGET)%
		;にせちん
		ELSE
			PRINTFORM 穿戴式假陰茎
		ENDIF
		PRINTFORML 直接挿入了%SLIT()%的最深処…
		PRINTFORML 
		;ちんちん
		IF TALENT:TARGET:2 & 2
			PRINTFORM 散発着热气的%CALLNAME:TARGET%的%TINKO(TARGET)%
		;にせちん
		ELSE
			PRINTFORM 無機質的穿戴式假陰茎
		ENDIF
		PRINTFORML 激烈攪弄着膣襞的感覚、讓%CALLNAME:PLAYER%喘息不已…
	ENDIF
ENDIF

;-------------------------------------------------
;132,被対面座位
;-------------------------------------------------
@MESSAGE_COM132
;抜かず
IF TCVAR:PLAYER:100 == TARGET && TARGET == TFLAG:103
	;対象が射精
	IF NOWEX:TARGET:11
		PRINTFORML %CALLNAME:PLAYER%被%CALLNAME:TARGET%抱在懷中向上頂着、
		PRINTFORML 因為%SAMEN_DESCRIPTION(TARGET)%射到膣内深処的感触而顫抖不已
		;調教者も射精
		SIF NOWEX:PLAYER:11
			PRINTFORM 同時高潮的%CALLNAME:PLAYER%的%TINKO(PLAYER)%也剧烈震動着、灼熱的%SAMEN_DESCRIPTION(MASTER)%在空中四散…
	;前ターンでＶ中出し
	ELSEIF TCVAR:PLAYER:102
		PRINTFORML %CALLNAME:PLAYER%還在被%CALLNAME:TARGET%持續蹂躙着膣肉
		IF TALENT:TARGET:2 & 2
			PRINTFORM %TINKO(TARGET)%
		ELSE
			PRINTFORM 穿戴式假陰茎
		ENDIF
		PRINTFORML 每次插入的時候都会讓子宮内的精液之海搖動、使%CALLNAME:PLAYER%陷入了恍惚状態
		PRINTFORML 看到這個樣子感到満足的%CALLNAME:TARGET%更加積極的用亀頭撞撃着蜜穴的弱点…
	ELSE
		PRINTFORML 從%CALLNAME:PLAYER%的膣内滑出的%TINKO(TARGET)%、完全看不出萎靡的樣子
		PRINTFORML 突然%CALLNAME:TARGET%緊緊地抱了過来、嘴唇重疊舌頭交纏再一次開始搖晃着腰…
	ENDIF
ELSE
	PRINTFORML %CALLNAME:PLAYER%的身体被%CALLNAME:TARGET%從正面抱起
	SIF TEQUIP:PLAYER:0 & 2
		PRINTFORM 通过%SHOW_BOTTOM(PLAYER,2)%的間隙
	PRINTFORM 把
	IF TALENT:TARGET:2 & 2
		PRINTFORM %TINKO(TARGET)%
	;にせちん
	ELSE
		PRINTFORM 穿戴式假陰茎
	ENDIF
	PRINTFORM 挿入了%SLIT()%
	;口枷の有無(予定)
	SIF !TEQUIP:PLAYER:口枷
		PRINT 一辺接吻一辺
	PRINTL 向上頂着腰…
	PRINTFORML 
	;ちんちん
	IF TALENT:TARGET:2 & 2
		PRINTFORM 散発着热气的%CALLNAME:TARGET%的%TINKO(TARGET)%
	;にせちん
	ELSE
		PRINTFORM 無機質的穿戴式假陰茎
	ENDIF
	PRINTFORML 激烈攪弄着膣襞的感覚、讓%CALLNAME:PLAYER%喘息不已…
ENDIF

;-------------------------------------------------
;133,被背面座位
;-------------------------------------------------
@MESSAGE_COM133
;抜かず
IF TCVAR:PLAYER:100 == TARGET && TARGET == TFLAG:103
	;対象が射精
	IF NOWEX:TARGET:11
		PRINTFORML %CALLNAME:PLAYER%被%CALLNAME:TARGET%抱在懷中向上頂着、
		PRINTFORML 因為從%TINKO(TARGET)%噴出的%SAMEN_DESCRIPTION(TARGET)%而渾身顫抖
		;調教者も射精
		SIF NOWEX:PLAYER:11
			PRINTFORM 同時高潮的%CALLNAME:PLAYER%的%TINKO(PLAYER)%也剧烈震動着、灼熱的%SAMEN_DESCRIPTION(TARGET)%在空中四散…
	;前ターンでＶ中出し
	ELSEIF TCVAR:PLAYER:102
		PRINTFORML %CALLNAME:PLAYER%還在被%CALLNAME:TARGET%持續蹂躙着膣肉
		IF TALENT:TARGET:2 & 2
			PRINTFORM %TINKO(TARGET)%
		ELSE
			PRINTFORM 穿戴式假陰茎
		ENDIF
		PRINTFORML 每次突入到深処、都会感到從結合処溢出了混雜着愛蜜和精子的淫液
		PRINTFORML 面対着因為中出的快感而委身的%CALLNAME:PLAYER%、%CALLNAME:TARGET%更加努力的頂着腰製作着孩子…
	ELSE
		PRINTFORML 從%CALLNAME:PLAYER%的膣内滑出的%TINKO(TARGET)%、完全看不出萎靡的樣子
		PRINTFORML 突然被%CALLNAME:TARGET%的手転動了身体、同時撫摸着這辺的肌膚…
	ENDIF
ELSE
	PRINTFORML %CALLNAME:PLAYER%的身体被%CALLNAME:TARGET%從背後抱起
	PRINTFORM 
	IF TEQUIP:PLAYER:0 & 2
		PRINTFORML 通过%SHOW_BOTTOM(PLAYER,2)%的空隙慢慢地把
		IF TALENT:TARGET:2 & 2
			PRINTFORM %TINKO(TARGET)%
		;にせちん
		ELSE
			PRINTFORM 穿戴式假陰茎
		ENDIF
		PRINTFORML 挿入了%SLIT()%…
	ELSE
		PRINTFORML 慢慢地用
		IF TALENT:TARGET:2 & 2
			PRINTFORM %TINKO(TARGET)%
		;にせちん
		ELSE
			PRINTFORM 穿戴式假陰茎
		ENDIF
		PRINTFORML 挿進了已経做好準備的%SLIT()%内…
	ENDIF
	PRINTFORML 
	;ちんちん
	IF TALENT:TARGET:2 & 2
		PRINTFORM 散発着热气的%CALLNAME:TARGET%的%TINKO(TARGET)%
	;にせちん
	ELSE
		PRINTFORM 無機質的穿戴式假陰茎
	ENDIF
	PRINTFORML 激烈攪弄着膣襞的感覚、讓%CALLNAME:PLAYER%喘息不已…
ENDIF
