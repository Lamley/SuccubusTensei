;-------------------------------------------------
;63,後背位肛交
;-------------------------------------------------
@MESSAGE_COM63
IF FLAG:70 == 1
	IF TFLAG:193
		;連続
		IF SELECTCOM == PREVCOM && ASSIPLAY == TFLAG:150
			;調教者についてる
			IF TALENT:PLAYER:2 & 2
				PRINTFORML 在品味着毫无抵抗的%CALLNAME:TARGET%肛门的触感的同时、再次開始動起了腰…
			;穿戴式假陰茎を使用している場合
			ELSE
				PRINTFORML 没有从毫无抵抗的%CALLNAME:TARGET%的肠内抜出、再次開始動起了腰…
			ENDIF
		;抜かず
		ELSEIF TCVAR:101 == PLAYER
			IF TEQUIP:体位２ == 2
				PRINTFORML 保持着対毫无抵抗的%CALLNAME:TARGET%肛门的插入再次開始動起了腰…
			;対面立位から
			ELSE
				PRINTFORML 暂时将%TINKO(PLAYER)%从%CALLNAME:TARGET%体内抜出并绕到后面
				PRINTFORML 将屁股摆成突出来的姿势再次開始侵犯起了%CALLNAME:TARGET%的肛门…
			ENDIF
		ELSE
			PRINTFORM 時停中的被摆成屁股突出来的姿势的%CALLNAME:TARGET%的
			IF TEQUIP:0 & 2
				PRINTFORM 拨開%SHOW_BOTTOM(TARGET,2)%、
			ELSEIF TEQUIP:0 & 1
				PRINTFORM %SHOW_BOTTOM(TARGET,1)%掀起来、
			ENDIF
			PRINTFORM 从后面用%TINKO(PLAYER)%挿入
			;潤滑LV4以上
			SIF PALAM:潤滑 >= PALAMLV:4
				PRINT 充満粘液的
			PRINTFORML 肛门的最深処…
		ENDIF
	ELSE
		;連続
		IF SELECTCOM == PREVCOM && ASSIPLAY == TFLAG:150
			;調教者についてる
			IF TALENT:PLAYER:2 & 2
				PRINTFORML 在品味着毫无抵抗的%CALLNAME:TARGET%肛门的触感的同时、再次開始動起了腰…
			;穿戴式假陰茎を使用している場合
			ELSE
				PRINTFORML 没有从毫无抵抗的%CALLNAME:TARGET%的肠内抜出、再次開始動起了腰…
			ENDIF
		;抜かず
		ELSEIF TCVAR:101 == PLAYER
			PRINTFORML 保持着挿入让%CALLNAME:TARGET%四肢着地趴下、再次開始動起了腰…
		ELSE
			PRINTFORM 対時停中被摆成四肢着地趴下姿势的%CALLNAME:TARGET%
			IF TEQUIP:0 & 2
				PRINTFORM 拨開%SHOW_BOTTOM(TARGET,2)%、
			ELSEIF TEQUIP:0 & 1
				PRINTFORM %SHOW_BOTTOM(TARGET,1)%掀起来、
			ENDIF
			PRINTFORM 从后面用%TINKO(PLAYER)%挿入
			;潤滑LV4以上
			SIF PALAM:潤滑 >= PALAMLV:4
				PRINT 充満粘液的
			PRINTFORML 肛门的最深処…
		ENDIF
	ENDIF
ELSEIF CFLAG:MASTER:イタズラ == 2
	IF TFLAG:193
		;連続
		IF SELECTCOM == PREVCOM && ASSIPLAY == TFLAG:150
			;調教者についてる
			IF TALENT:PLAYER:2 & 2
				PRINTFORML 在品味着睡眠中的%CALLNAME:TARGET%肛门的触感的同时、再次開始動起了腰…
			;穿戴式假陰茎を使用している場合
			ELSE
				PRINTFORML 没有从睡眠中的%CALLNAME:TARGET%的肠内抜出、再次開始動起了腰…
			ENDIF
		;抜かず
		ELSEIF TCVAR:101 == PLAYER
			IF TEQUIP:体位２ == 2
				PRINTFORML 保持着対睡眠中的%CALLNAME:TARGET%肛门的插入再次開始動起了腰…
			;対面立位から
			ELSE
				PRINTFORML 暂时将%TINKO(PLAYER)%从%CALLNAME:TARGET%体内抜出并绕到后面
				PRINTFORML 将屁股摆成突出来的姿势再次開始侵犯起了%CALLNAME:TARGET%的肛门…
			ENDIF
		ELSE
			PRINTFORM 対睡眠中被擺成了凸出臀部姿勢的%CALLNAME:TARGET%
			IF TEQUIP:0 & 2
				PRINTFORM 拨開%SHOW_BOTTOM(TARGET,2)%、
			ELSEIF TEQUIP:0 & 1
				PRINTFORM %SHOW_BOTTOM(TARGET,1)%掀起来、
			ENDIF
			PRINTFORM 从后面用%TINKO(PLAYER)%挿入
			;潤滑LV4以上
			SIF PALAM:潤滑 >= PALAMLV:4
				PRINT 充満粘液的
			PRINTFORML 肛门的最深処…
		ENDIF
	ELSE
		;連続
		IF SELECTCOM == PREVCOM && ASSIPLAY == TFLAG:150
			;調教者についてる
			IF TALENT:PLAYER:2 & 2
				PRINTFORML 在品味着睡眠中的%CALLNAME:TARGET%肛门的触感的同时、再次開始動起了腰…
			;穿戴式假陰茎を使用している場合
			ELSE
				PRINTFORML 没有从睡眠中的%CALLNAME:TARGET%的肠内抜出、再次開始動起了腰…
			ENDIF
		;抜かず
		ELSEIF TCVAR:101 == PLAYER
			PRINTFORML 保持着挿入让%CALLNAME:TARGET%趴着、再次開始動起了腰…
		ELSE
			PRINTFORM 対着趴着睡眠中的%CALLNAME:TARGET%
			IF TEQUIP:0 & 2
				PRINTFORM 拨開%SHOW_BOTTOM(TARGET,2)%、
			ELSEIF TEQUIP:0 & 1
				PRINTFORM %SHOW_BOTTOM(TARGET,1)%掀起来、
			ENDIF
			PRINTFORM 从后面用%TINKO(PLAYER)%挿入
			;潤滑LV4以上
			SIF PALAM:潤滑 >= PALAMLV:4
				PRINT 充満粘液的
			PRINTFORML 肛门的最深処…
		ENDIF
	ENDIF
ELSE
	IF TFLAG:193
		;連続
		IF SELECTCOM == PREVCOM && ASSIPLAY == TFLAG:150
			;調教者についてる
			IF TALENT:PLAYER:2 & 2
				PRINTFORML 在品味着%CALLNAME:TARGET%肛门的触感的同时、再次開始動起了腰…
			;穿戴式假陰茎を使用している場合
			ELSE
				PRINTFORML 没有从%CALLNAME:TARGET%的肠内抜出、再次開始動起了腰…
			ENDIF
		;抜かず
		ELSEIF TCVAR:101 == PLAYER
			IF TEQUIP:体位２ == 2
				PRINTFORML 保持着対%CALLNAME:TARGET%肛门的插入再次開始動起了腰…
			;対面立位から
			ELSE
				PRINTFORML 暫時從%CALLNAME:TARGET%体内抜出%TINKO(PLAYER)%并\@ OPENPLACE(CFLAG:MASTER:現在位置) == 2 ? 抓住%CALLNAME:TARGET%伸出的双手 # 讓%CALLNAME:TARGET%扶住墻壁 \@
				PRINTFORML 从后面插入进去并再次開始動起了腰…
			ENDIF
		ELSE
			IF TEQUIP:0 & 2
				PRINTFORM 拨開%SHOW_BOTTOM(TARGET,2)%
			ELSEIF TEQUIP:0 & 1
				PRINTFORM 掀起%SHOW_BOTTOM(TARGET,1)%
			ENDIF
			PRINTFORM %CALLNAME:TARGET%
			PRINTFORM \@ OPENPLACE(CFLAG:MASTER:現在位置) == 2 ? 抓住伸出的双手 # 的両手扶着墻壁 \@、從後面挿入到了
			;潤滑LV4以上
			SIF PALAM:潤滑 >= PALAMLV:4
				PRINT 充満粘液的
			PRINTFORML 肛门的最深処…
		ENDIF
	ELSE
		;連続
		IF SELECTCOM == PREVCOM && ASSIPLAY == TFLAG:150
			;調教者についてる
			IF TALENT:PLAYER:2 & 2
				PRINTFORML 在品味着%CALLNAME:TARGET%肛门的触感的同时、再次開始動起了腰…
			;穿戴式假陰茎を使用している場合
			ELSE
				PRINTFORML 没有从%CALLNAME:TARGET%的肠内抜出、再次開始動起了腰…
			ENDIF
		;抜かず
		ELSEIF TCVAR:101 == PLAYER
			PRINTFORML 保持着挿入状态让%CALLNAME:TARGET%四肢着地趴下、再次開始動起了腰…
		ELSE
			PRINTFORM 対四肢着地趴下的%CALLNAME:TARGET%
			IF TEQUIP:0 & 2
				PRINTFORM 拨開%SHOW_BOTTOM(TARGET,2)%、
			ELSEIF TEQUIP:0 & 1
				PRINTFORM %SHOW_BOTTOM(TARGET,1)%掀起来、
			ENDIF
			PRINTFORM 从后面用%TINKO(PLAYER)%挿入
			;潤滑LV4以上
			SIF PALAM:潤滑 >= PALAMLV:4
				PRINT 充満粘液的
			PRINTFORML 肛门的最深処…
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;323,相手主導後背位Ａ
;-------------------------------------------------
@MESSAGE_COM323
PRINTFORML %CALLNAME:MASTER%從後面圧在%CALLNAME:TARGET%身上、把亀頭対準了\@ ABL:肛 > 2? 因為欲求而微顫的# \@肛門、慢慢地沈下了腰……
PRINTFORML %CALLNAME:TARGET%仔細品味着挿入感并発出了嬌喘……